# Feed 社交系统面经

> 目标：整理这个项目最容易被问到的面试题，覆盖底层原理、算法设计、系统设计、代码实现、故障排查和追问链路。
>
> 阅读方式：先看“高频必问”，再看“追问延展”，最后背“项目亮点和坑点”。

---

## 1. 项目一句话介绍

这是一个基于 Go + Vue3 的社交内容系统，核心是“动态时间线 + 私信 + 通知中心”。后端使用 MySQL、Redis、RabbitMQ 做持久化、缓存和异步分发，前端用 Vue3 + Pinia + Router 实现完整交互。

### 面试时怎么说

“这个项目最核心的点不是简单 CRUD，而是围绕动态时间线做了推拉混合分发、MQ 异步消费、Redis 缓存、游标分页和幂等控制，重点解决的是高并发下的可用性和扩展性。”

---

## 2. 高频必问题

### 2.1 这个项目最核心的技术难点是什么？

**参考回答：**

最核心的难点是动态时间线的分发和读取。

普通社交系统如果所有用户发帖都“直接推给所有粉丝”，在粉丝量大的场景下会产生写放大问题；如果完全“读的时候临时拉取”，又会导致读延迟高、列表合并复杂。

所以我做的是**推拉混合模型**：
- 普通用户发动态后，通过 MQ 异步推送到粉丝 inbox
- 大 V 动态不做全量 push，只写自己的 outbox
- 用户刷时间线时，把 own + inbox + 大 V outbox 合并后去重排序，再做游标分页

这个方案的本质是用“写扩散”和“读扩散”做权衡。

**追问 1：为什么大 V 要走 pull？**

因为大 V 的粉丝量通常远高于普通用户，如果每发一条都同步推给所有粉丝，会产生极大的写放大和延迟放大。普通用户粉丝少，push 成本可控；大 V 粉丝多，适合把数据先存到 outbox，用户刷首页时再按需拉取。

**追问 2：为什么不用全 push？**

全 push 会把发布接口变成“写粉丝收件箱”的重操作，粉丝越多越慢，而且还会把 Redis、MQ、DB 的压力集中到发布路径上，影响主流程可用性。

**追问 3：推拉混合怎么保证排序正确？**

先把 own、inbox、outbox 三路候选 ID 合并去重，再回表按 `created_at DESC, id DESC` 统一排序。这样不依赖来源顺序，而依赖最终排序规则，能保证整体时间线稳定。

**追问 4：如果 Redis 挂了怎么办？**

Redis 主要是加速层，不是唯一真相源。挂了以后可以回源 MySQL 拉取用户资料、动态详情和最近动态；时间线性能会下降，但核心功能还能继续用。

---

### 2.2 你这个时间线为什么不用 offset 分页，而是 cursor 分页？

**参考回答：**

因为 offset 分页在动态流场景下有明显问题：

1. **性能问题**：偏移量越大，数据库扫描越多。
2. **一致性问题**：在新增/删除数据时，前后页容易重复或丢失。
3. **体验问题**：用户刷时间线时，数据变化很快，offset 容易跳页。

所以我使用的是**游标分页**：
- 游标由 `created_at_unix_Milli|feed_id` 组成
- 通过“时间倒序 + ID 倒序”保证稳定排序
- 下一页使用 `nextCursor` 继续向更旧内容翻页

这类分页更适合 feed、消息流、通知流等高频变化的列表。

**追问 1：cursor 为什么要带 ID，不只带时间戳行不行？**

因为同一时间戳可能有多条动态，只靠时间戳会出现翻页边界不稳定的问题。加上 ID 后可以形成唯一且稳定的排序边界，避免重复或漏数据。

**追问 2：如果同一毫秒有多条记录怎么办？**

用 `created_at DESC, id DESC` 作为联合排序。时间相同时用 ID 再做一次倒序，保证顺序唯一且可复现。

**追问 3：cursor 分页和 keyset pagination 的关系？**

本质上是一回事。都是利用“上一页最后一条记录的排序键”作为下一页查询条件，避免 offset 扫描大量无关数据。

---

### 2.3 为什么动态分发用了 RabbitMQ？

**参考回答：**

RabbitMQ 的作用是把“发布动态”和“分发动态”解耦。

如果用户发布时直接同步推送到所有粉丝：
- 发布接口会被粉丝数拖慢
- 某个粉丝 DB/Redis 异常会影响发布主流程
- 发布链路和分发链路耦合太重，不利于扩展

所以我让发布接口只负责：
- 写 MySQL 主表
- 记录 Bloom Filter
- 投递 MQ 消息

真正的分发逻辑由 consumer 异步执行，这样可以：
- 削峰填谷
- 降低发布接口延迟
- 支持重试、死信队列、幂等

**追问 1：Kafka 和 RabbitMQ 怎么选？**

RabbitMQ 更适合业务型消息、路由灵活、上手快；Kafka 更适合超高吞吐、日志流和事件流。这个项目更偏业务分发，所以选 RabbitMQ 更直接。

**追问 2：为什么不用 Redis Stream？**

Redis Stream 也能做消息队列，但在重试、死信、可观测性、路由能力上，RabbitMQ 更成熟，且和当前项目的异步任务模型更匹配。

**追问 3：MQ 丢消息怎么办？**

我这边做了发布失败降级、消费重试和死信队列兜底。即使 MQ 短暂不可用，也不会阻塞发帖主流程。

**追问 4：如何保证消息至少一次或恰好一次？**

实际工程里通常保证“至少一次”，然后靠幂等消费来消除重复处理。恰好一次在分布式场景成本很高，通常不是第一选择。

---

### 2.4 你这个项目怎么保证 MQ 消费幂等？

**参考回答：**

我用的是 **Redis SETNX + TTL** 做幂等标记。

每条消息都会有一个唯一 key，形如：
`feed:{feed_id}:author:{author_id}`

消费前先尝试 `SETNX`：
- 成功：说明首次处理，继续执行
- 失败：说明这条消息已经处理过，直接忽略

TTL 的作用是防止幂等 key 永久占用空间，同时允许一段时间后重新处理历史消息。

**追问 1：为什么不是数据库唯一键？**

数据库唯一键只能限制单表单列的重复插入，而 MQ 消费幂等通常跨多个动作（比如写 inbox、写 outbox、写 timeline）。Redis SETNX 更适合作为“这条消息是否处理过”的统一标记。

**追问 2：TTL 设太短会怎样？**

幂等 key 过早过期后，同一条历史消息再次投递时可能被重复处理。TTL 需要覆盖消息可能重放的窗口，不能太短。

**追问 3：如果消费成功但 ack 前挂了，会不会重复消费？**

会有可能，所以更需要幂等控制。RabbitMQ 语义通常是至少一次投递，重复消费必须在业务侧兜住。

**追问 4：幂等和去重有什么区别？**

去重更偏“数据集合里不要重复出现”；幂等更偏“同一个操作执行多次，结果保持一致”。消费链路里更需要幂等。

---

### 2.5 你怎么判断一个用户是不是大 V？

**参考回答：**

当前项目里大 V 由用户表中的 `is_big_v` 字段表示，且支持缓存。

同时我有一个更新逻辑：
- 根据 `follower_count` 和配置阈值判断是否达到大 V 标准
- 如果状态变化，就更新数据库和缓存
- 这样保证“读写一致性”尽量靠近业务真实状态

**追问 1：大 V 标记是人工配置还是自动计算？**

可以两种都支持。当前实现里更偏向自动判断 follower_count 是否超过阈值，但最终落库成 `is_big_v`，这样查询更快。

**追问 2：为什么要缓存 bigv 状态？**

因为时间线里会频繁判断关注对象是不是大 V，如果每次都查数据库会增加额外开销。缓存可以让这个判断更快。

**追问 3：如果缓存过期了怎么办？**

可以回源数据库再确认，并在必要时回填缓存。这样不会因为缓存失效影响业务正确性。

**追问 4：follower_count 和 is_big_v 不一致怎么办？**

以数据库持久化状态为准，定时或在关键路径做修正，避免缓存和真实状态长期偏离。

---

## 3. 底层原理类题目

### 3.1 Bloom Filter 为什么能用来判断“动态是否存在”？

**参考回答：**

Bloom Filter 是一个概率型集合结构，特点是：
- 空间小
- 查询快
- 允许“假阳性”，不允许“假阴性”

在项目里，我把 userID 和 feedID 写进 Bloom Filter：
- 请求用户资料 / 动态详情时先查 Bloom Filter
- 如果 Bloom 说“不存在”，就可以直接返回 404，减少 DB 访问
- 如果 Bloom 说“可能存在”，再去 Redis/DB 回源确认

它非常适合做“是否大概率存在”的第一道过滤。

**追问方向：**
- 假阳性怎么理解？
- 为什么不会假阴性？
- 位图大小和误判率怎么权衡？
- 数据删除后 Bloom Filter 怎么处理？

---

### 3.2 Redis 为什么适合做 inbox / outbox？

**参考回答：**

因为 inbox / outbox 本质上都是“按时间排序的 feed ID 集合”，而 Redis 的 ZSet 很适合：
- 按 score 排序
- 范围查询
- 倒序读取
- 成员去重

我这里用时间戳作为 score，feedID 作为 member：
- `inbox:{userID}` 表示别人推给我的动态
- `outbox:{authorID}` 表示我自己发出去、后续要被拉取的动态

ZSet 的优势是读时间线非常快，尤其适合“最近 N 条”这种读法。

**追问 1：为什么不用 List？**

因为 List 更适合先进先出，但不适合按时间范围过滤和去重。ZSet 可以直接按 score 做范围查询和排序，更符合时间线需求。

**追问 2：ZSet 插入复杂度是多少？**

通常是 O(logN)，在大多数 feed 场景下比全量扫描和重排序更合适。

**追问 3：score 冲突怎么办？**

score 相同的时候，可以依赖 feedID 做二级排序，保证最终顺序稳定。

**追问 4：Redis 挂了如何兜底？**

可以回源 MySQL 拉最近动态，时间线会慢一些，但不会完全不可用。

---

### 3.3 为什么“时间线”要做 own + inbox + outbox 合并？

**参考回答：**

因为首页时间线实际上不是单一来源，而是多个来源的混合结果：

1. **own**：自己发的内容，必须可见
2. **inbox**：普通关注对象 push 过来的内容
3. **outbox**：关注的大 V 没有全量 push，需要读的时候拉

合并后还要：
- 去重
- 排序
- 游标分页

这是一个典型的“多路归并”问题。

**追问 1：为什么不先存成一个统一 timeline？**

统一存成单一时间线会让写入和回收逻辑更复杂，也会增加不同来源的策略差异处理。拆成 own/inbox/outbox 更容易按业务策略扩展。

**追问 2：多路归并的复杂度是多少？**

核心复杂度取决于候选集大小和排序方式。通常先在 ID 层合并去重，再回表排序，比直接对完整对象做全量合并更可控。

**追问 3：如何保证大 V 内容不重复？**

通过 ID 去重和 cursor 边界过滤避免重复。即使同一 feed 同时出现在多个来源，也只会保留一次。

---

### 3.4 令牌桶限流的底层原理是什么？

**参考回答：**

令牌桶的核心思想是：
- 系统按固定速率往桶里放令牌
- 请求到来时必须拿到令牌才能继续执行
- 桶满了就丢弃多余令牌
- 桶空了请求就被限流

它适合控制突发流量，因为允许一定的瞬时高峰。

项目里它用于：
- 登录/注册：按 IP 限制
- 发布/评论/转发/私信：按用户限制

**追问 1：令牌桶和漏桶区别？**

令牌桶允许突发流量，只要桶里有令牌就能放行；漏桶更强调匀速输出，流量会被强制平滑掉。

**追问 2：为什么不用固定窗口计数？**

固定窗口在窗口边界会出现突刺问题，比如窗口切换瞬间可能放行双倍流量。令牌桶更平滑，也更符合真实业务波动。

**追问 3：Redis 怎么保证分布式限流一致性？**

通过 Redis 原子操作或 Lua 脚本统一读写令牌状态，保证并发下不会超发令牌。

---

### 3.5 为什么要用缓存击穿/雪崩保护？

**参考回答：**

因为项目里有很多热点读：
- 用户资料
- 动态详情
- 时间线首页

如果热点 key 过期，大量请求会同时打到数据库，造成击穿。

所以用了：
- TTL 随机抖动，避免同一时间大面积过期
- singleflight 合并回源请求，避免并发击穿
- 写操作后主动失效缓存，保证读到最新值

**追问 1：缓存穿透、击穿、雪崩的区别？**

穿透是查不存在的数据，每次都打到 DB；击穿是热点 key 失效后大量请求打到 DB；雪崩是大量 key 同时过期导致数据库被冲垮。

**追问 2：singleflight 的原理？**

同一个 key 的并发回源请求会合并成一次实际执行，其余请求等待这次结果返回，避免重复回源。

**追问 3：失效和更新，哪个更适合这里？**

这里更偏向“写后失效”。因为点赞、评论、编辑都属于更新频繁且需要较强一致性的场景，主动失效更简单也更稳。

---

## 4. 算法设计类题目

### 4.1 时间线分页的核心算法怎么设计？

**参考回答：**

时间线分页本质上是一个“多源有序集合的分页归并问题”。

我的实现思路是：

1. 解析 cursor，得到时间边界和 ID 边界
2. 拉取候选 feed ID：
   - 自己的动态
   - inbox
   - 关注大 V 的 outbox
3. 对候选 ID 去重
4. 回表查 feed，并按 `created_at DESC, id DESC` 排序
5. 取 `pageSize + 1` 条判断是否还有下一页
6. 根据最后一条生成 nextCursor

这个方案能保证：
- 排序稳定
- 分页连续
- 重复数据最小化

**追问 1：为什么不是直接从 Redis 里合并？**

因为 Redis 里只有 ID 和 score，最终还要回表拿作者、点赞状态、转发原文等完整信息。先合并 ID 再回表更适合做统一排序和数据补全。

**追问 2：多路归并怎么去重？**

先在 ID 层做 unique，再按时间和 ID 排序。这样即使同一条 feed 同时出现在 own、inbox 或 outbox 中，也只会保留一次。

**追问 3：为什么要先查候选 ID 再查 DB？**

因为候选集往往比完整动态表小得多，先收窄 ID 范围可以减少数据库扫描范围，也便于做统一排序和分页边界控制。

---

### 4.2 推拉混合分发怎么设计？

**参考回答：**

推拉混合是社交 feed 的经典设计。

- **push**：发帖时把动态推给粉丝 inbox，适合粉丝少的普通用户
- **pull**：粉丝阅读时拉作者 outbox，适合粉丝很多的大 V

这样做的原因是：
- 普通用户适合写时扩散，读快
- 大 V 不适合全量推送，写放大太严重

所以系统按身份和负载做策略分流。

**追问 1：这个阈值怎么定？**

通常根据粉丝数、写入成本、发布延迟和资源压力综合设定。阈值不是固定真理，而是一个需要结合业务增长动态调整的经验值。

**追问 2：push 和 pull 怎么保证最终一致？**

通过异步分发、幂等消费、回源兜底和定时补偿来保证最终一致性。即使某次推送失败，也可以在后续读取时从 outbox 或 DB 补回来。

**追问 3：大 V 变普通用户、普通用户变大 V 怎么处理？**

要同步更新用户身份标记，并根据新状态调整分发策略。切换时通常需要做一轮历史数据补偿，避免旧策略残留。

---

### 4.3 点赞、评论、转发的计数为什么要事务处理？

**参考回答：**

因为它们是典型的“写记录 + 改计数”的复合操作。

例如点赞：
- 插入 like 记录
- 增加 feed.like_count

这两个动作必须保持一致，所以要放在同一个事务里。
如果只成功了一半，就会出现：
- 有点赞记录但计数没加
- 计数加了但记录没写

事务可以保证原子性。

**追问 1：事务隔离级别怎么选？**

一般会根据一致性和并发量做权衡。这个项目里更偏向保证写入正确性和简单可控，通常不需要上到特别高的隔离级别，但要避免脏读、脏写这类明显问题。

**追问 2：为什么计数不完全放 Redis？**

Redis 可以做计数加速，但最终落库还是必要的，因为 MySQL 才是主数据源。完全放 Redis 会带来持久化和恢复复杂度。

**追问 3：大量点赞会不会成为热点行？**

会，所以需要考虑计数写入的热点问题。常见做法是结合缓存、异步聚合或分片计数，避免所有请求都直接打同一行。

---

### 4.4 私信系统的未读数怎么设计？

**参考回答：**

私信本质是“会话 + 消息”的结构。

通常会有：
- message 表存消息内容
- conversation 表存会话状态和未读计数

用户发消息后：
- 落库
- 如果接收者在线，通过 WebSocket 立即推送
- 否则保留在数据库，用户下次拉取

未读数可以在消息写入时同步维护，也可以异步聚合。

**追问 1：已读/未读状态怎么设计？**

可以在会话表里记录最后已读消息 ID 或最后已读时间戳，列表页直接根据这个位置判断哪些消息未读。

**追问 2：WebSocket 断线重连怎么办？**

断线后重新连接时先做认证，再拉取会话列表和历史消息，确保不会漏消息；如果有未发送成功的消息，也可以通过客户端重放或服务端补偿。

**追问 3：消息顺序如何保证？**

通常按服务端创建时间和消息 ID 排序，必要时在消息链路里加入严格递增的序列值，避免并发下顺序错乱。

---

## 5. 系统设计类题目

### 5.1 如果用户量扩大 10 倍，你最先优化哪里？

**参考回答：**

我会优先优化三块：

1. **时间线分发**
   - 大 V 拉取改进
   - inbox/outbox 扩容和裁剪策略
   - 分发任务异步化

2. **缓存层**
   - 热点 feed/user 缓存
   - 更完善的失效策略
   - 防击穿/防雪崩

3. **数据库层**
   - 索引优化
   - 读写分离
   - 历史消息和动态归档

**追问 1：你先扩 Redis 还是先拆 MySQL？**

优先看瓶颈在哪里。通常先保证热点读走缓存，再看数据库是否已经是写入瓶颈；如果 DB 压力明显，就要考虑读写分离和分表分库。

**追问 2：如何观测瓶颈？**

主要看接口耗时、DB 慢查询、Redis 命中率、MQ 堆积长度和 CPU/内存使用率。先通过监控定位是读慢、写慢还是分发慢。

**追问 3：哪些接口最容易成为热点？**

首页时间线、动态详情、点赞/评论列表、用户资料和私信会话列表通常最容易成为热点接口。

---

### 5.2 如果 RabbitMQ 挂了怎么办？

**参考回答：**

我做了两个层面的兜底：

1. **发布侧熔断降级**
   - MQ 发布失败后短期开路
   - 直接只写 outbox，保证发布主链路不阻塞

2. **消费侧重试/DLQ**
   - 消费失败进入重试队列
   - 超过阈值进入死信队列
   - 幂等 key 避免重复处理

这样即使 MQ 异常，系统也能维持基本可用性。

**追问 1：熔断时间怎么定？**

熔断时间一般设置为一个短暂窗口，比如十几秒到几十秒，目的是避免连续失败拖垮发布链路，同时给 MQ 恢复留出时间。

**追问 2：降级后数据如何补偿？**

降级后可以由后台补偿任务或定时任务把缺失的分发数据重新补上，保证最终一致性。

**追问 3：DLQ 怎么人工回放？**

死信队列里的消息可以人工排查后重新投递到主队列，或者通过管理脚本批量回放。

---

### 5.3 如果 Redis 挂了，系统还能不能用？

**参考回答：**

能用，但性能会下降。

因为 Redis 主要承担：
- 缓存
- inbox/outbox
- 幂等标记
- 限流
- Bloom Filter

Redis 挂了以后：
- 动态详情会回源 MySQL
- 时间线会回源 DB 拉取最近数据
- 幂等能力下降，但 MQ 仍可通过 DB 或逻辑兜底

也就是说，Redis 是**性能增强层**，不是唯一存储。

**追问 1：哪些功能依赖 Redis 最强？**

最依赖的是动态详情缓存、时间线 inbox/outbox、幂等标记和限流。它们都能明显降低数据库压力。

**追问 2：Redis 故障对一致性有什么影响？**

主要影响的是读性能和部分加速能力，不会立刻破坏主存储一致性，因为主数据仍在 MySQL。

**追问 3：有没有本地缓存兜底？**

可以有，但这个项目里更偏向直接回源 MySQL 和利用 Redis 作为分布式缓存层，本地缓存一般只作为进一步优化手段。

---

### 5.4 你这个项目怎么保证“读到的数据尽量最新”？

**参考回答：**

我采用的是“写后失效 + 读时回源 + 缓存回填”的策略。

比如：
- 点赞、评论、编辑、删除后，主动删除 feed detail 缓存
- 下一次请求先查缓存，没有就回源 DB
- 回源成功后回填缓存

这样可以保证热点数据在性能和一致性之间取得平衡。

**追问 1：为什么不是写穿？**

写穿会让每次写操作都必须同步更新缓存，写路径会变长，而且在复杂场景下不一定比失效简单可靠。

**追问 2：为什么不是写回？**

写回适合延迟写入场景，但会带来一致性窗口和丢数据风险。这个项目里的动态详情更适合写后失效。

**追问 3：你能接受多长时间的数据延迟？**

对于点赞、评论这种交互，通常希望秒级甚至更低延迟；缓存失效后立刻回源，可以把延迟控制在一个很短的范围内。

---

## 6. 追问链路题目

### 6.1 为什么 `PublishFeed` 不直接写 inbox/outbox？

**参考回答：**

因为发布接口应该保持轻职责，只负责入库和投递事件。

真正的分发应该在 MQ consumer 中做，这样：
- 发布接口更快
- 分发逻辑可以重试
- 错误不会直接阻塞用户发帖
- 便于扩展普通用户和大 V 的不同策略

**追问 1：发布接口和 consumer 职责怎么划分？**

发布接口只做“写主表 + 发事件”，consumer 负责“消费事件 + 写 inbox/outbox + 维护分发状态”，这样职责边界更清楚。

**追问 2：为什么不在 service 层判断大 V？**

可以判断，但真正的分发动作还是应该在异步消费里做。这样 service 层不会被大 V 粉丝规模拖慢，也更利于重试和补偿。

---

### 6.2 为什么大 V 不直接 push 到所有粉丝？

**参考回答：**

因为大 V 的粉丝很多，push 成本太高，会造成写放大。

如果一个大 V 有几十万粉丝：
- 每发一条都要写几十万次 inbox
- MQ、Redis、DB 都会很重
- 发布延迟会飙升

所以大 V 走 outbox，阅读时再拉取，这是典型的降写放大方案。

**追问 1：这个阈值如何定？**

通常根据历史流量、单次发布写放大成本和系统容量来定。可以先给一个保守阈值，再根据监控动态调整。

**追问 2：如果大 V 粉丝突然暴涨怎么办？**

要有动态切换策略：当粉丝数超过阈值后切换到 pull 模式，并通过缓存标记或配置中心快速生效。

---

### 6.3 为什么要同时维护 `inbox` 和 `timeline` 表？

**参考回答：**

因为它们承担的角色不一样：
- `inbox`：Redis 中的读取加速结构
- `timeline`：MySQL 中的持久化记录，方便回溯和兜底

如果只有 Redis，一旦 Redis 故障就会丢读取路径；
如果只有 DB，读时间线会更慢。

所以两个都维护，形成“快路径 + 慢路径兜底”。

**追问 1：双写一致性怎么保证？**

通常会通过事务、消息重试和补偿任务来保证最终一致。不能完全依赖单次写入成功，要考虑失败后的补写。

**追问 2：哪个先写，哪个后写？**

一般先写主存储，再异步补充缓存或收件箱，这样主数据先落稳，再做加速层更新。

---

### 6.4 为什么 `GetTimelineByCursor()` 要先 own，再 inbox，再 outbox？

**参考回答：**

因为用户自己的内容一定要优先展示，这部分是最稳定、最符合预期的。

其次是普通用户 push 到 inbox 的内容，最后才是大 V pull 过来的 outbox 内容。

这个顺序有两个好处：
- 语义清晰
- 发生异常时，自己的内容最不容易丢

**追问 1：如果 inbox/outbox 中有重复 feed 怎么办？**

先做 ID 去重，再统一排序，这样相同 feed 不会重复展示。

**追问 2：为什么不用统一排序之后再分来源展示？**

也可以统一排序，但来源拆分更方便做不同策略，比如大 V 拉取、普通用户 push、个人内容优先等。

---

## 7. 数据一致性和故障处理题

### 7.1 你这里最容易出现的数据不一致场景是什么？

**参考回答：**

最容易出问题的是多存储层之间的一致性：

- MySQL 已经写成功，但 Redis 没更新
- Redis 写成功，但 DB timeline 没写成功
- MQ 消息已经消费，但幂等 key 过期
- 大 V 状态更新后缓存没同步

这些都属于分布式系统里典型的一致性问题。

我的策略是：
- 主数据以 MySQL 为准
- Redis 作为加速层和可回源层
- consumer 幂等
- 关键写操作后主动失效缓存

**追问 1：一致性最终怎么收敛？**

通过主库最终落地、异步补偿、幂等消费和缓存失效来收敛。系统不追求每一步都强一致，但要保证最终状态一致。

**追问 2：能不能做到强一致？为什么？**

分布式环境下跨 MySQL、Redis、MQ 做强一致代价很高，性能和复杂度都会大幅上升。这个项目更适合采用最终一致性方案。

---

### 7.2 如果某条消息消费成功，但 ack 前宕机，会怎样？

**参考回答：**

RabbitMQ 会认为这条消息没有被确认，于是它可能会重新投递。

这就是为什么消费端必须做幂等控制：
- 即使重复投递，也不能重复写 inbox/outbox/timeline
- Redis SETNX 幂等 key 就是为了这个场景

**追问 1：幂等 key 过期后会不会重复处理？**

有这个风险，所以幂等 key 的 TTL 需要结合消息重放窗口来设定，不能过短。

**追问 2：如果 Redis 也坏了，怎么办？**

可以依赖数据库唯一约束、业务状态标记或者后续补偿任务继续兜底，但处理成本会更高，所以 Redis 幂等通常还是很重要。

---

### 7.3 你如何处理“删除动态”后的级联清理？

**参考回答：**

删除动态要同时清理：
- MySQL 主表
- feed detail 缓存
- 作者 outbox
- 粉丝 inbox
- timeline 记录

我现在是先删主表，再异步清理相关 timeline 和 inbox，避免阻塞主流程。

**追问 1：异步清理失败怎么办？**

可以通过重试队列、补偿任务或者定时巡检把残留数据补齐删除，避免脏数据长期存在。

**追问 2：你怎么避免脏数据残留？**

删除动作后要有幂等清理机制和补偿机制，确保即使某一步失败，也能在后续任务里再次清理。

---

## 8. 代码实现题

### 8.1 说说 `GetTimelineByCursor()` 的实现细节

**参考回答：**

它的流程大致是：

1. 规范 pageSize，限制最大值
2. 解析 cursor
3. 拉取自己最近的 feed
4. 读取 inbox 的 feed ID
5. 读取关注大 V 的 outbox feed ID
6. 合并去重
7. 回表查询 feeds
8. 按 `created_at DESC, id DESC` 排序
9. 生成 nextCursor
10. 返回响应和 hasMore

关键点是：
- 不是直接把 Redis 结果返回给前端
- 最终还是要回表构造完整响应，避免 N+1

**追问 1：为什么要回表？**

因为 Redis 里通常只存 feed ID 和排序信息，真正展示还需要作者、点赞状态、评论数、转发原文等完整字段，所以最终还是要回表组装响应。

**追问 2：去重应该在 ID 层还是对象层做？**

优先在 ID 层去重，更轻量也更容易统一排序。对象层去重成本更高，而且会让逻辑更复杂。

---

### 8.2 你为什么要在 feed response 里再查作者和原文？

**参考回答：**

因为 feed 响应不是单表字段，需要展示：
- 作者信息
- 原文信息
- 点赞状态
- 评论状态

如果逐条查会有 N+1 问题，所以我在 `buildFeedResponses()` 里做了批量加载：
- 批量查作者
- 批量查原文
- 批量查点赞状态

这样比每条 feed 都单独查数据库更高效。

**追问 1：如何进一步优化 N+1？**

继续把查询批量化，比如作者、原文、点赞状态都尽量一次性查出，再在内存里组装成最终响应。

**追问 2：有没有考虑预组装 DTO？**

有，DTO 预组装可以让返回结构更稳定，也更适合前端直接消费。这个项目里实际上已经在往 DTO 化方向整理。

---

### 8.3 你这个项目怎么避免 feed 重复展示？

**参考回答：**

重复展示主要通过两层处理：

1. **ID 去重**
   - own、inbox、outbox 合并后先做 unique

2. **游标条件过滤**
   - 只取比当前 cursor 更旧的数据
   - 再按时间 + ID 排序

这样可以降低重复展示概率。

**追问 1：如果同一条 feed 同时出现在 inbox 和 outbox 怎么办？**

先做统一去重，保留一份即可。因为前面已经按 ID 去重，所以同一条 feed 只会进入最终列表一次。

**追问 2：如果 Redis 和 DB 里都有同一条记录呢？**

最终以 DB 回表结果为准，Redis 只是候选来源或加速结构，重复来源在上游合并时就应该被消掉。

---

## 9. 面试官常问的“为什么不用……？”

### 9.1 为什么不用纯 push？

因为粉丝量大的用户会导致写放大严重，发布接口延迟高，系统扛不住。

### 9.2 为什么不用纯 pull？

因为纯 pull 每次首页都要查很多关注用户，合并成本高，读延迟更大。

### 9.3 为什么不用 offset？

因为 offset 在动态流里容易重复、漏数据，而且越往后性能越差。

### 9.4 为什么不用 MySQL 一张表直接搞定时间线？

因为首页时间线是多源、多策略、强实时场景，单表查询既慢又难扩展。

### 9.5 为什么不用 Redis 直接存完整 feed 对象？

因为 Redis 更适合做高频读加速和集合索引，完整对象还是要落 MySQL，保证持久化和可恢复性。

---

## 10. 项目亮点总结词

你可以这样收尾：

“这个项目不是单纯的 CRUD 项目，而是一个围绕社交 feed 构建的高并发系统。我重点做了 MQ 异步分发、推拉混合时间线、Redis 缓存、Bloom Filter、游标分页、幂等重试和降级熔断，核心目标是把写扩散、读扩散、可用性和一致性之间的权衡做平衡。”

---

## 11. 面试时建议优先背的 10 题

1. 为什么时间线用 cursor 分页？
2. 为什么动态分发要用 MQ？
3. 什么是推拉混合？为什么大 V 走 pull？
4. 怎么保证 MQ 消费幂等？
5. Bloom Filter 的原理是什么？
6. Redis 挂了系统还能不能用？
7. 为什么点赞评论要放事务里？
8. 如何处理 inbox / outbox / timeline 的一致性？
9. `GetTimelineByCursor()` 如何保证排序和去重？
10. 项目里最大的性能优化点是什么？

---

## 12. 最后给自己的回答模板

### 模板 1：原理题

“这个设计本质上是在做 XXX 和 YYY 的权衡。因为在高并发场景下，如果直接使用 A，会导致 B 问题，所以我采用了 C 方案。C 的优点是……，代价是……”

### 模板 2：追问题

“这里我不是只看功能实现，还考虑了失败场景和扩展性，所以额外做了……。如果再往下扩展，我会进一步做……”

### 模板 3：算法题

“这个问题可以抽象成一个……问题，核心步骤是……，时间复杂度是……，为了保证稳定性我还加了……”

---

> 如果你愿意，下一版我可以继续给你补一份：
> **《这个项目的高频八股题标准答案版》**
> 直接按“面试官问法 -> 标准答法 -> 追问 -> 深挖点”格式写。

---

## 13. 追问补充答案

### 13.1 为什么大 V 要走 pull？

因为大 V 的粉丝量通常远高于普通用户，如果每发一条都同步推给所有粉丝，会产生极大的写放大和延迟放大。普通用户粉丝少，push 成本可控；大 V 粉丝多，适合把数据先存到 outbox，用户刷首页时再按需拉取。

### 13.2 为什么不用全 push？

全 push 会把发布接口变成“写粉丝收件箱”的重操作，粉丝越多越慢，而且还会把 Redis、MQ、DB 的压力集中到发布路径上，影响主流程可用性。

### 13.3 推拉混合怎么保证排序正确？

先把 own、inbox、outbox 三路候选 ID 合并去重，再回表按 `created_at DESC, id DESC` 统一排序。这样不依赖来源顺序，而依赖最终排序规则，能保证整体时间线稳定。

### 13.4 Redis 挂了怎么办？

Redis 主要是加速层，不是唯一真相源。挂了以后可以回源 MySQL 拉取用户资料、动态详情和最近动态；时间线性能会下降，但核心功能还能继续用。

### 13.5 cursor 为什么要带 ID，不只带时间戳？

因为同一时间戳可能有多条动态，只靠时间戳会出现翻页边界不稳定的问题。加上 ID 后可以形成唯一且稳定的排序边界，避免重复或漏数据。

### 13.6 同一毫秒有多条记录怎么办？

用 `created_at DESC, id DESC` 作为联合排序。时间相同时用 ID 再做一次倒序，保证顺序唯一且可复现。

### 13.7 MQ 丢消息怎么办？

发布侧有失败重试和熔断降级，消费侧有重试队列和死信队列兜底。同时消费前会做幂等控制，所以即使重复投递也不会重复写数据。

### 13.8 为什么不是数据库唯一键做幂等？

数据库唯一键只能限制单表单列的重复插入，而 MQ 消费幂等通常跨多个动作（比如写 inbox、写 outbox、写 timeline）。Redis SETNX 更适合作为“这条消息是否处理过”的统一标记。

### 13.9 TTL 设太短会怎样？

幂等 key 过早过期后，同一条历史消息再次投递时可能被重复处理。TTL 需要覆盖消息可能重放的窗口，不能太短。

### 13.10 假阳性和假阴性怎么理解？

Bloom Filter 可能说“存在”但其实不存在，这叫假阳性；但不会把存在的元素说成不存在，这叫假阴性。用于“先拦截明显不存在的请求”非常合适。

### 13.11 为什么点赞评论要放事务里？

因为它们是“写记录 + 改计数”的组合操作，必须原子化。事务可以保证要么都成功，要么都失败，避免计数和实际记录不一致。

### 13.12 为什么要主动失效缓存而不是等过期？

因为点赞、评论、编辑、删除都属于强一致性较高的写操作。主动失效能让下一次读立即回源拿到最新数据，比等待 TTL 到期更安全。

### 13.13 为什么时间线不用 offset？

offset 会随着页数增加扫描越来越多，而且在数据频繁插入/删除时容易出现重复和漏数据。cursor 分页更适合动态流。

### 13.14 为什么要批量查作者和点赞状态？

避免 N+1 查询。列表页可能有很多条动态，如果每条都单独查作者、点赞和原文，数据库压力会非常大，所以统一批量加载后再组装响应。

### 13.15 为什么评论接口现在不用 map 了？

因为 map 灵活但不稳定，字段容易散、容易错。改成 `CommentResponse` 后，前后端结构更统一，也方便维护和补字段。

### 13.16 为什么 `Author` 字段之前保留，现在又去掉了？

如果嵌套对象能表达更完整语义，就可以保留；如果只是为了展示昵称，扁平字段更简单。后面我把点赞和评论都改成了扁平结构，前端读起来更直接。

### 13.17 为什么不直接用 `FeedResponse` 装评论？

因为评论不是动态本体，语义不对。`FeedResponse` 适合动态列表，评论最好单独定义 `CommentResponse`，这样扩展回复、楼中楼时不会污染动态 DTO。

### 13.18 关注链路为什么会影响首页？

首页不是只看自己发的内容，而是由 own、inbox、outbox 拼出来的。如果关注关系没写进数据库，或者关注人的 feed 没写入 inbox，首页就只会显示自己的动态。

### 13.19 为什么你后来查到 inbox 有数据却读不出来？

通常是 score 单位不一致或者 cursor 范围不一致导致的。写入和读取必须统一时间单位，否则 Redis 里明明有数据，查询范围却对不上。

### 13.20 为什么最后统一成 `Username` / `Nickname` 扁平字段？

因为前端展示主要用昵称，扁平结构更容易消费，也能避免每次都套一层 `Author` 再 `ToResponse()`。这样既统一又省事。
