# Feed 项目学习文档（详细版）

> 更新时间：2026-04-10  
> 目标：作为该项目的学习、复盘、面试讲解、二次开发参考手册。

---

## 1. 项目定位与学习目标

`Feed` 是一个偏“小红书/微博”风格的社交系统，覆盖了社交产品中最常见的一整套能力：

- 用户体系（注册/登录/JWT）
- 关注关系（关注/粉丝）
- 内容系统（发动态、评论、点赞、转发）
- 时间线（推拉结合）
- 私信 + WebSocket 实时通信
- 通知中心
- Redis 缓存与限流
- RabbitMQ 异步分发（重试、DLQ、幂等）

### 学这个项目你能掌握什么

1. **完整业务链路设计**：从 API 到服务层，再到缓存/MQ 的工程落地。  
2. **高并发思路**：热点缓存、限流、异步化、重试、降级、熔断。  
3. **社交系统核心难点**：时间线分发策略、消息未读、通知聚合。

---

## 2. 技术栈与职责划分

## 后端（Go）

- 框架：Gin
- ORM：GORM + MySQL
- 缓存：Redis
- 消息队列：RabbitMQ
- 鉴权：JWT

**后端职责**：

- 提供 REST API
- 执行业务规则（权限、状态流转、幂等等）
- 管理数据一致性（DB + 缓存 + MQ）
- 对外提供实时推送能力（WebSocket）

## 前端（Vue3）

- Vue 3 + Composition API
- Vue Router
- Pinia
- Axios
- Element Plus
- Vite

**前端职责**：

- 页面渲染与交互
- 状态管理（登录态、用户信息、消息状态）
- 调用 API 与错误反馈
- WebSocket 消息展示

---

## 3. 项目结构速览（按职责理解）

```text
feed/
├─ backend/
│  ├─ handlers/      # 路由处理层（参数解析、返回结构）
│  ├─ services/      # 业务核心层（主要逻辑都在这里）
│  ├─ repository/    # 数据访问封装（DB查询组织）
│  ├─ models/        # 数据模型与索引定义
│  ├─ middleware/    # 鉴权、限流等中间件
│  ├─ mq/            # MQ生产消费、重试、DLQ、熔断
│  ├─ realtime/      # WebSocket相关
│  ├─ cache/         # 缓存封装
│  ├─ router/        # 路由注册
│  └─ config/        # 配置加载
├─ frontend/
│  └─ src/
│     ├─ views/      # 页面（timeline、publish、messages...）
│     ├─ components/ # 复用组件
│     ├─ api/        # 接口请求封装
│     ├─ stores/     # pinia状态
│     └─ router/     # 前端路由
└─ study.md
```

---

## 4. 核心功能详解（重点）

## 4.1 用户体系（注册/登录/JWT）

### 目标
保证“谁在操作”可识别，后续所有写操作都可鉴权。

### 典型流程
1. 用户提交账号密码注册。  
2. 登录成功后后端签发 JWT。  
3. 前端保存 token（通常放本地存储 + 请求头）。  
4. 后续请求带上 token，后端中间件解析并注入 user_id。

### 设计要点
- 密码加密存储（不可明文）。
- token 过期机制 + 无效 token 处理。
- 用户信息接口和登录态解耦（前端可单独刷新用户资料）。

---

## 4.2 关注关系（Follow Graph）

### 目标
建立用户间“内容传播关系”，为时间线分发提供基础。

### 核心操作
- 关注某用户
- 取关某用户
- 查询某用户粉丝/关注列表

### 数据与性能点
- 关系读取频繁，可用 Redis Set 缓存关注关系。
- 对于“是否已关注”这类判断，可优先走缓存减少 DB 压力。

---

## 4.3 动态系统（Feed）

### 功能点
- 发布动态（文本/图片/视频）
- 删除动态
- 动态详情
- 点赞/取消点赞
- 评论/删评论
- 转发

### 关键业务规则
- 删除权限：仅作者本人可删。
- 点赞幂等：重复点赞不应重复计数。
- 评论分页：按时间倒序，避免一次加载过大。

### 索引思路
项目已补充关键索引，例如：
- `feeds(created_at, user_id)`：支持时间线与用户动态列表。
- `likes(feed_id, user_id)`：支持点赞去重和快速查询。
- `comments(feed_id, created_at)`：支持评论分页。

---

## 4.4 时间线（最核心）

时间线是社交系统最关键模块。该项目采用 **推拉混合（Hybrid Fanout）**。

### 为什么不用纯推/纯拉
- 纯推（写扩散）：读快，但大V发帖会写爆。
- 纯拉（读扩散）：写轻，但读时聚合成本高。

### 当前策略
- **普通用户**：写扩散（push 到粉丝 inbox）
- **大V用户**：读扩散（只写自己 outbox，读取时再拉取）

这样做实现了：
- 普通场景读性能高
- 大V场景写入可控

### 返回结构设计
时间线 API 支持游标分页：
- `cursor`
- `next_cursor`
- `has_more`

这是比 offset 分页更稳定的方案，适合持续新增数据场景。

### 你要重点理解的点
1. 多来源数据合并（own + push + pull）。  
2. 去重（避免同一条动态重复）。  
3. 按时间统一排序。  
4. 游标计算要可持续翻页。

---

## 4.5 私信与实时通信（WebSocket）

### 功能点
- 发送私信
- 会话列表
- 会话消息记录
- 未读计数
- 实时消息推送（`message:new`）

### 常见设计
- 消息先落库再推送（保证可追溯）。
- 推送失败不影响落库成功（弱依赖处理）。
- 未读计数可结合 Redis 做快速聚合。

### 面试可讲
“为什么用 WS？”
- 因为私信需要低延迟、服务端主动推送，不适合纯轮询。

---

## 4.6 通知中心

### 通知类型
- like
- comment
- follow

### 功能
- 通知分页列表
- 未读数统计
- 全部已读

### 设计要点
- 通知是“衍生数据”，适合异步生成（通过 MQ）。
- 对用户感知来说，允许轻微延迟，但要保证最终可见。

---

## 4.7 搜索中心

### 当前实现
- 搜索用户
- 搜索动态
- 前端按 Tab 分类展示

### 优化策略（已做）
- 从 `%keyword%` 调整为 `keyword%` 前缀匹配，尽量利用索引。

### 后续可升级
- MySQL FullText
- Elasticsearch

---

## 4.8 可靠性能力（工程价值高）

## MQ 可靠投递
- 主队列
- 重试队列
- 死信队列（DLQ）

消费失败后按策略重试，超过阈值进入 DLQ，方便排查与补偿。

## 幂等处理
消费端使用 Redis `SETNX + TTL` 防重复消费，避免重复写 timeline/通知。

## 熔断与降级
- MQ 发布异常：触发短期开路，避免请求堆积。
- 熔断期间：降级为仅写 outbox，主流程可继续。
- 消费写 DB 异常：可降级处理，尽量不阻断整条链路。

---

## 4.9 缓存与限流（高并发基础）

## 缓存
- 用户资料缓存
- 动态详情缓存
- 写后主动失效关键缓存
- TTL 随机抖动防雪崩
- `singleflight` 防缓存击穿

## 限流
采用 Redis 令牌桶限流：
- IP 维度：登录/注册
- 用户维度：发布/转发/评论/私信

效果：抗刷、防恶意请求、保护核心数据库。

---

## 5. 前端交互亮点（当前版本）

1. 左侧导航 + 中间主内容布局。  
2. 顶部搜索入口联动发现页。  
3. 已做 UI 调整：
   - 去掉右上角个人菜单
   - 左下角新增“设置”
   - 设置中支持“退出登录 + 深色模式”
4. 深色模式支持本地持久化，刷新后保留状态。

---

## 6. 关键 API 记忆清单

### Auth
- `POST /api/auth/register`
- `POST /api/auth/login`

### User
- `GET /api/users/me`
- `PUT /api/users/me`
- `GET /api/users/search`

### Follow
- `POST /api/follow/:id`
- `DELETE /api/follow/:id`

### Feed / Timeline
- `POST /api/feeds`
- `GET /api/feeds/:id`
- `GET /api/timeline`

### Like / Comment
- `POST /api/feeds/:id/like`
- `DELETE /api/feeds/:id/like`
- `POST /api/feeds/:id/comments`

### Message / WS
- `POST /api/messages`
- `GET /api/messages/conversations`
- `GET /ws/messages?token=xxx`

### Notification
- `GET /api/notifications`
- `POST /api/notifications/read-all`

---

## 7. 建议学习路径（按周）

## 第 1 周：跑通系统
- 启动 MySQL/Redis/RabbitMQ
- 跑后端 + 前端
- 亲自走完：注册 -> 登录 -> 发动态 -> 关注 -> 看时间线

## 第 2 周：啃核心服务
- 重点看：`feed_service.go`
- 画出“发动态到时间线”的完整时序图
- 把 push/pull 边界条件写成笔记

## 第 3 周：稳定性专题
- 梳理 MQ 重试与 DLQ 流程
- 看熔断/降级触发条件
- 理解幂等键设计与 TTL 选择

## 第 4 周：自己做一版增强
可选练手：
- 点赞/评论计数异步聚合
- 更完整的暗色主题
- 接入监控（Prometheus + Grafana）

---

## 8. 面试讲解模板（可直接背）

“这是一个 Go + Vue 的社交系统，核心亮点是时间线采用推拉混合：普通用户写扩散，大V读扩散，平衡了写放大和读放大。系统使用 Redis 做缓存和令牌桶限流，RabbitMQ 做异步分发，并配套重试、DLQ 与幂等控制，保证在高并发下可用性和一致性。实时私信通过 WebSocket 推送，通知中心作为异步衍生数据，整体链路兼顾性能和工程可靠性。”

---

## 9. 下一步优化清单（建议）

1. 计数器异步化（点赞/评论写 Redis 后批量回刷 DB）。  
2. 搜索升级到 ES（高亮、排序、召回策略）。  
3. 增加消息可靠投递追踪（trace id + 可观测日志）。  
4. 时间线缓存分层（热用户专门策略）。  
5. 完善端到端压测脚本与容量评估。

---

## 10. 后端排障实战章节（可直接用于面试与值班）

> 目标：把“我会写功能”升级为“我能扛线上故障”。

---

### 10.1 通用排障方法论（先止血，再定位，再复盘）

发生故障时，优先级顺序：

1. **止血**：限流、降级、熔断、回滚、扩容，先恢复可用性。  
2. **定位**：看日志、指标、调用链，确认故障边界（单机/集群、单接口/全站）。  
3. **修复**：发布补丁或配置修复，验证恢复。  
4. **复盘**：补监控、补告警、补演练、补自动化。

一个实用口诀：**现象 -> 影响面 -> 最近变更 -> 依赖健康 -> 资源瓶颈 -> 数据一致性**。

---

### 10.2 场景一：服务崩了怎么办（panic / 进程退出 / 反复重启）

#### 常见现象
- API 大面积 5xx
- 实例频繁重启，日志出现 panic stack
- 上游超时激增

#### 快速排查步骤
1. 看进程与重启次数（容器/k8s/systemd）。
2. 看最近错误日志，确认是 panic 还是 OOMKill。
3. 看最近发布记录，判断是否变更引入。
4. 若某依赖不可用（Redis/MQ/DB），先降级相关路径。

#### 项目落地点（建议）
- 增加 `recover` 中间件，记录 panic 栈并返回统一错误。
- 增加 `/healthz`（活性）与 `/readyz`（就绪）。
- 接入优雅停机：收到 SIGTERM 后停止接新请求并等待 in-flight。

#### 面试可讲
“我把服务故障处理拆成三步：入口兜底（recover）+ 探针隔离（readiness）+ 平滑重启（graceful shutdown），避免单点抖动演变成全站雪崩。”

---

### 10.3 场景二：内存泄漏怎么办（RSS持续上涨、GC频繁）

#### 常见现象
- 内存曲线持续上升不回落
- GC 次数飙升，延迟抖动
- 最终 OOM，被系统杀死

#### 快速排查步骤
1. 开启 `pprof`，抓 `heap` 和 `goroutine` 快照。
2. 对比故障前后快照，找增长最快的对象类型。
3. 检查是否 goroutine 泄漏（阻塞在 channel、ticker 未 stop、无超时 IO）。
4. 压测复现并验证修复效果。

#### 项目落地点（建议）
- 暴露 debug 端点（仅内网可访问）。
- 对异步任务统一加 `context timeout`。
- 规范 `ticker.Stop()`、连接关闭、defer 释放。

#### 面试可讲
“我用 pprof 对比两次 heap/goroutine 快照，定位到某异步分支未设置超时导致 goroutine 累积，修复后内存曲线恢复锯齿形。”

---

### 10.4 场景三：死锁怎么办（数据库死锁/锁等待）

#### 常见现象
- 接口偶发超时
- 日志出现 deadlock / lock wait timeout
- 并发高时错误率明显上升

#### 快速排查步骤
1. 从 DB 日志确认死锁 SQL 与事务。
2. 找到冲突资源（同表同索引范围）。
3. 检查事务范围是否过大、是否有反向加锁顺序。
4. 对可重试事务加有限重试（指数退避）。

#### 项目落地点（建议）
- 对点赞/评论等热点写路径，统一更新顺序。
- 缩短事务：只包必要 SQL，不把外部调用放入事务。
- 死锁错误码识别后自动重试 2~3 次。

#### 面试可讲
“我把高并发写路径做了固定加锁顺序，并把事务范围缩到最小，再配合短重试，把死锁从偶发错误变成可恢复抖动。”

---

### 10.5 场景四：缓存崩了怎么办（Redis不可用/缓存雪崩）

#### 常见现象
- Redis 超时或连接失败
- DB QPS 突增，接口整体变慢
- 热点接口抖动严重

#### 快速排查步骤
1. 判断是 Redis 全挂、分片故障还是网络抖动。
2. 立即开启限流，保护 DB。
3. 启用降级路径（缓存 miss/失败 -> DB 回源）。
4. 观察回源比例和 DB 资源，必要时临时降级部分功能。

#### 你项目已体现的点
- timeline 读取 inbox 失败时会 fallback DB。
- 大 V 标识与 outbox 读取失败有回源逻辑。
- 详情读取用 `singleflight` 防击穿。

#### 建议再补
- 本地短 TTL 缓存（热点 key 二级缓冲）。
- 熔断器（连续失败快速失败，避免拖垮线程池）。
- 过期时间抖动，避免同刻大量失效。

#### 面试可讲
“Redis 故障时我们优先保核心读链路：限流 + 回源 + 热点保护，牺牲部分实时性换取整体可用。”

---

### 10.6 场景五：慢 SQL 排查与优化

#### 常见现象
- 某接口 P95/P99 突增
- DB CPU 高、IO 高、线程堆积
- 应用侧超时增多

#### 快速排查步骤
1. 从慢 SQL 日志定位 Top N 语句（按总耗时与次数）。
2. 用 `EXPLAIN` 看执行计划：是否走索引、是否回表、是否 filesort。
3. 判断是索引问题、SQL写法问题，还是分页策略问题。
4. 逐项优化并压测对比（P50/P95/QPS）。

#### 典型优化手段
- 建联合索引（匹配 where + order by）。
- 避免深分页 offset，改游标分页（你项目时间线已采用游标思路）。
- 消除 N+1（你在 feed 批量构建中已做优化）。
- 仅查必要字段，减少回表成本。

#### 面试可讲
“我按慢日志先做 TopN，再用 EXPLAIN 定位 filesort/回表，结合联合索引和游标分页把 P95 明显降下来。”

---

### 10.7 建议补充的监控与告警清单

至少要有以下看板和告警：

- **应用层**：QPS、错误率、P95/P99、goroutine、heap、GC pause
- **DB 层**：慢 SQL 数、连接池占用、锁等待、死锁次数
- **Redis 层**：命中率、超时数、连接数、拒绝数
- **MQ 层**：堆积长度、消费失败率、重试次数、DLQ 增速

告警原则：
- 先告“用户受损”指标（错误率、延迟）
- 再告“资源异常”指标（CPU/内存/连接）

---

### 10.8 一段可直接背的“排障能力”表述

“我在这个项目里不只做了业务功能，还重点补了故障处理能力：服务异常时有 recover、健康检查和优雅停机；缓存故障时有回源与限流保护；高并发写路径用事务最小化和重试降低死锁影响；慢 SQL 通过慢日志 + EXPLAIN + 索引/分页优化闭环处理；并通过 pprof 和核心监控指标实现可观测、可复盘、可演练。”

---

## 11. 总结

这个项目已经具备“可上线社交系统”的核心骨架。学习时不要只看接口，要重点吃透四件事：

1. **时间线分发策略**（业务核心）
2. **缓存 + 限流 + 异步化**（性能核心）
3. **重试 + 幂等 + 降级**（稳定性核心）
4. **可观测 + 排障 + 复盘**（工程核心）

把这四块讲清楚，你的项目深度会明显高于普通 CRUD 项目，也更贴近真实后端工程实践。