# Feed 项目面试题整理（含详细回答）

> 说明：这份题单基于当前项目已实现的功能、代码结构和常见面试追问方式整理。回答部分尽量结合你这个项目的实际实现，同时补充背后的理论取舍，方便你直接背诵或再加工成自己的表达。

---

## 一面：基础理解 + 项目主链路

### 1. 自我介绍

**问题**
- 你在这个项目里主要负责了哪些模块？
- 你最能代表这个项目深度的一块内容是什么？
- 如果让你用 1 分钟介绍这个项目，你会怎么讲？

**参考回答**
我在这个 Feed 项目里主要负责后端核心业务链路，重点包括用户认证、关注关系、Feed 时间线、Redis 缓存、令牌桶限流、RabbitMQ 异步消息，以及 WebSocket 私信这几个模块。整体上我不是只做 CRUD，而是围绕“社交系统的核心链路”去设计，尤其是时间线的推拉结合、消息的异步分发和实时推送，这些都是这个项目最能体现工程能力的地方。

如果 1 分钟介绍这个项目，我会这样说：这是一个类似微博/小红书的社交系统，核心能力包括用户体系、关注关系、动态发布、时间线、评论点赞、私信、通知中心和搜索。后端采用 Gin + GORM + MySQL + Redis + RabbitMQ 架构，Redis 负责缓存、限流和部分关系数据，RabbitMQ 负责异步通知和 Feed 分发，WebSocket 负责私信实时通信。项目最核心的设计是时间线推拉结合，并通过 Outbox Pattern 保证 Feed 写库和异步分发事件的一致性：普通用户采用写扩散，大 V 采用读扩散，从而平衡写入成本和读取性能。

从面试角度看，这个回答要体现三点：你知道项目做了什么，你知道你负责了什么，你知道这些模块背后的设计原因，而不是只会报接口名。

---

### 2. 为什么选择做这个项目？核心想解决什么问题？

**问题**
- 这个项目对应的业务场景是什么？
- 你为什么选社交系统，而不是电商、博客或者 IM？
- 这个项目里你自己最想证明的技术点是什么？

**参考回答**
我选择社交系统，是因为它比单纯 CRUD 项目更能体现真实后端工程能力。社交系统的核心问题不只是“把数据存进去”，而是要解决内容如何高效传播、如何做缓存、如何做异步、如何做实时通信、如何保证幂等和一致性。比如动态发布后，如何快速进入粉丝时间线；私信如何低延迟送达；热点用户发帖时如何避免写爆；通知如何异步生成。这些问题都很典型，也更接近真实线上系统。

相比电商或博客，社交系统更适合展示“高并发 + 实时性 + 分布式”的综合能力。电商很多场景更偏订单和交易一致性，博客又相对简单，而社交系统天然包含关注图谱、信息流、实时消息和通知，这些都更有挑战。

我最想证明的技术点主要有三个：第一是时间线的推拉混合设计，第二是 Redis + RabbitMQ 的异步与缓存体系，第三是 WebSocket 私信的实时通信设计。也就是说，我希望面试官看到的不只是功能完成，而是我能理解系统为什么这样设计、为什么这样拆分、以及每个方案的优缺点。

---

### 3. 项目的整体架构是怎么分层的？

**问题**
- handler、service、middleware、cache、mq、realtime 各自负责什么？
- 为什么要做这样的分层？
- 你觉得哪一层最容易写乱？为什么？

**参考回答**
这个项目采用的是比较典型的分层架构：

- `handler` 负责接收请求、解析参数、返回响应，属于接口层。
- `service` 负责承载核心业务规则，例如发动态、关注、消息发送、时间线合并等，属于业务层。
- `middleware` 负责统一横切逻辑，比如鉴权、限流、CORS。
- `cache` 负责 Redis 访问封装，把缓存 key、缓存策略、TTL、集合操作统一收口。
- `mq` 负责消息队列生产和消费，处理异步通知、重试、死信、幂等等。
- `realtime` 负责 WebSocket 连接管理和实时消息推送。

这样分层的好处是职责清晰。handler 不会混业务逻辑，service 不会掺杂 HTTP 细节，缓存策略和 MQ 细节也不会散落在各处。对后期维护来说，分层之后更容易扩展，比如要改缓存策略，只需要改 cache 层或 service 层少量调用，不会影响所有 handler。

最容易写乱的是 service 层，因为它处在中间，既要处理业务规则，又要协调 DB、缓存、MQ、实时推送。如果没有边界意识，很容易把事务逻辑、缓存逻辑、消息逻辑全写在一起，最后变成“大杂烩”。所以这个项目里 service 层一定要遵守一个原则：核心业务流程放这里，但不要把具体基础设施细节完全散开。

---

### 4. 登录认证是怎么做的？

**问题**
- 为什么使用 JWT？
- token 是怎么从请求里取出来并校验的？
- token 过期之后系统怎么处理？

**参考回答**
项目使用 JWT 作为登录认证方案，主要原因是它天然适合分布式和多实例部署。JWT 的优点是服务端不需要维护 session 状态，token 本身携带了用户身份信息和过期时间，只要签名验证通过，就能在任意实例上完成鉴权。这对后端水平扩容特别友好。

在请求处理时，中间件会从请求头或请求参数中解析 token，再进行签名校验和过期校验。校验通过后把当前用户 ID 写入 Gin 的上下文，这样后面的 handler 和 service 就能通过统一方法读取当前登录用户，而不需要每个接口都重复解析 token。

token 过期之后，系统会直接返回未授权或 token 过期的错误。对于 WebSocket 场景，连接建立后还会在运行过程中持续检查 token 过期时间，一旦过期，就主动给客户端发送 `auth:expired` 事件并关闭连接，避免用户拿着过期身份继续使用实时通道。

从理论上讲，JWT 的代价是服务端无法天然“主动注销”单个 token，所以如果需要踢下线或强制失效，通常要配合黑名单、token 版本号或短有效期 + 刷新机制。当前项目主要采用的是短时有效的访问 token 模式。

---

### 5. 用户体系有哪些核心接口？

**问题**
- 注册、登录、获取当前用户、更新资料分别做了什么？
- 用户资料为什么适合做缓存？
- 哪些用户信息不适合长期缓存？

**参考回答**
用户体系的核心接口包括注册、登录、获取当前用户、更新资料和查询用户信息。注册负责创建账号并做基础校验，登录负责校验密码并签发 JWT，获取当前用户用于前端初始化登录态，更新资料用于修改昵称、头像、简介等可变信息。

用户资料适合做缓存，是因为它属于典型的“读多写少”数据。用户在浏览首页、个人页、评论页时会频繁读取头像、昵称、简介等信息，而这些内容不会频繁变化，因此可以放到 Redis 里减少数据库压力。项目里也有对用户资料缓存的封装，并且带了 TTL 随机抖动，避免大量 key 在同一时刻过期引发缓存雪崩。

不适合长期缓存的，一般是强实时或强一致的字段，比如账号状态、封禁状态、权限等级、风控标记、临时登录状态等。这类数据如果缓存过久，可能导致权限判断错误。对于这种数据，要么不缓存，要么设置较短 TTL，要么在更新时主动失效。

---

### 6. 关注关系是怎么设计的？

**问题**
- 关注、取关、粉丝、关注列表分别怎么存取？
- 你为什么把关注关系放到 Redis 里做缓存？
- 如何判断“是否关注过某用户”？

**参考回答**
关注关系本质上是一张图谱关系表，核心就是“谁关注了谁”。数据库负责最终一致性，Redis 负责高频查询加速。粉丝列表、关注列表、是否关注过某用户，这些都是社交系统里非常高频的读取场景，所以很适合缓存。

在实现上，关注和取关会先更新数据库，再同步更新 Redis 中的粉丝集合和关注集合，保证大部分读取直接命中缓存。判断“是否关注过某用户”时，可以直接使用 Redis Set 的成员判断，因为集合查询是 O(1) 的，适合这种布尔型频繁查询。

把关注关系缓存到 Redis 的原因有两个：一是降低数据库压力，二是便于时间线分发。因为时间线推送需要知道一个用户的粉丝是谁，如果粉丝列表能快速读取，就能更快把动态写入粉丝 inbox。理论上，关注关系属于典型的关系型数据 + 缓存加速场景，数据库保证正确性，Redis 保证性能。

---

### 7. 动态系统的核心功能有哪些？

**问题**
- 发动态、删动态、详情、点赞、评论、转发分别怎么走链路？
- 哪些操作必须做权限校验？
- 哪些操作要做幂等？

**参考回答**
动态系统是整个项目的内容核心。它包含发布动态、删除动态、动态详情、点赞、取消点赞、评论、删除评论、转发等功能。发布动态时，后端不仅要写入动态表，还要根据用户类型决定是否要同步进入粉丝时间线。删除动态时，需要做作者权限校验，确保只有本人能删除自己的内容。点赞和评论则属于热点写操作，需要重点考虑幂等和性能。

权限校验主要出现在三类操作里：删除动态、删除评论、修改个人资料或执行敏感写操作。因为这些操作必须确认“是本人发起”，不能只看是否登录，还要判断资源归属。

幂等最典型的是点赞、取消点赞、关注、取关、消息发送确认等操作。幂等的意义是：即使同一个请求被重复提交，也不会造成重复点赞、重复关注、重复计数。理论上，幂等可以通过唯一索引、Redis 去重键、状态机或条件更新来实现。这个项目里很多写路径都需要特别注意这一点，因为社交系统的用户操作很容易因重试、网络抖动或前端重复点击而重复触发。

---

### 8. 时间线是怎么设计的？

**问题**
- 为什么采用推拉结合，而不是纯推或纯拉？
- 普通用户和大 V 用户分别走什么策略？
- 为什么要区分 inbox 和 outbox？

**参考回答**
时间线是这个项目最核心的设计之一。纯推模式意味着用户发布内容后要立刻把内容分发到所有粉丝的收件箱，这样读起来很快，但当大 V 粉丝数很多时，写放大会非常严重。纯拉模式则相反，写入简单，但用户每次刷新时间线都要聚合大量数据，读开销大。于是项目采用推拉结合，平衡写放大和读放大。

普通用户粉丝数不多，发布动态后适合走推模式，也就是把 feed 直接写入粉丝 inbox，提升读取速度。大 V 用户粉丝数多，适合走拉模式，发布时只写自己的 outbox，读取时间线时再合并 outbox、inbox 以及必要的来源数据。

区分 inbox 和 outbox 的目的是把“别人的内容”和“自己的内容”拆开管理。inbox 是收件箱，存放从关注对象推来的内容；outbox 是发件箱，存放自己发布的内容。这样在时间线读取时，可以更灵活地做合并、排序和去重，也更方便针对大 V 采取不同策略。

理论上，这种设计属于社交系统中的经典 fanout on write / fanout on read 混合方案，核心价值就是在用户规模和内容传播规模不断增长时，仍然能把系统维持在可控范围内。

---

### 9. 时间线读取时是怎么合并数据的？

**问题**
- 自己发的内容、关注对象的内容、缓存内容分别怎么处理？
- 去重是在哪一步做的？
- 为什么采用游标分页，而不是 offset 分页？

**参考回答**
时间线读取时，通常要把多个来源的数据合并起来：一部分是自己发布的内容，一部分是关注对象推来的内容，另一部分可能来自缓存或 outbox。合并后要做统一排序，通常按照发布时间倒序。为了避免同一条内容因为多条来源重复出现，还要做去重，去重一般会基于 feedID 或唯一内容 ID。

游标分页比 offset 分页更适合时间线这种持续新增的数据场景。offset 分页的问题是，当中间插入新数据后，分页结果会漂移，可能出现重复或遗漏。游标分页则以某个时间点或某个 ID 作为边界，更适合“连续往下翻”的信息流场景。虽然实现复杂一点，但稳定性更高。

理论上，时间线合并本质上就是多路归并：保证排序稳定、去重稳定、分页稳定。这个项目里采用游标分页的思路是正确的，因为社交信息流天然是动态变化的，offset 在这种场景里体验并不好。

---

### 10. Redis 在项目里承担了哪些角色？

**问题**
- 哪些数据放缓存，哪些数据不放？
- 为什么要给缓存 TTL 加随机抖动？
- 什么场景下会有缓存击穿、雪崩、穿透？你是怎么考虑的？

**参考回答**
Redis 在这个项目里承担了多重角色。第一是缓存层，缓存用户资料、动态详情、关注列表等高频读取数据；第二是数据结构层，保存 inbox、outbox 这种有序集合；第三是限流层，用来实现令牌桶；第四是辅助一致性层，比如幂等键、临时标识、是否大 V 的标识等。

不是所有数据都适合放缓存。适合缓存的是读多写少、允许短暂不一致、且可回源的数据，比如用户资料、feed 详情、关注关系。像订单、支付状态、风控结果、权限等级这种强一致或敏感数据，就不适合长缓存。

TTL 加随机抖动是为了防缓存雪崩。比如一批 key 在同一时刻写入，又在同一时刻过期，就可能让大量请求同时打到数据库。加入随机抖动后，过期时间分散，系统压力更平滑。

缓存击穿、雪崩、穿透是三个经典问题：击穿是热点 key 失效瞬间大量请求回源；雪崩是大量 key 同时失效；穿透是查询一个根本不存在的数据，绕过缓存直打数据库。这个项目里通过 TTL 抖动、热点缓存、回源控制、令牌桶限流等方式降低这些风险。

---

### 11. 令牌桶限流是怎么实现的？

**问题**
- 登录、注册、发动态、评论这些接口为什么要限流？
- IP 维度和用户维度限流有什么区别？
- 限流失败后返回了什么信息？

**参考回答**
令牌桶是一种非常适合互联网业务的限流算法。它支持一定程度的突发流量，同时控制平均速率。这个项目里登录、注册、发动态、评论、转发、私信等接口都可以加限流，因为这些接口一旦被刷，容易带来安全风险、数据库压力或用户体验问题。

IP 维度限流更偏向防刷和防攻击，适合登录、注册这类匿名或弱身份接口。用户维度限流更偏向约束已登录用户的高频操作，比如发动态、评论、转发、私信。前者用于保护入口，后者用于保护业务核心。

限流失败后，系统会返回 429，并带上提示信息和 `Retry-After` 头，告诉客户端大概多久之后可以再试。这是比较标准的做法，也方便前端做体验上的提示。

理论上，令牌桶比固定窗口更平滑，能避免窗口边界上的突刺；也比简单计数更适合真实业务，因为真实用户操作并不是完全均匀的。

---

### 12. 上传功能是怎么做的？

**问题**
- 图片和视频的上传校验有哪些？
- 为什么只允许部分后缀？
- 如果多实例部署，上传目录会带来什么问题？

**参考回答**
上传功能主要做了类型校验、大小校验和路径管理。图片和视频分别有不同的最大大小限制，同时只允许指定的文件后缀，比如 jpg、png、gif、webp、mp4、mov 等。这样做的目的是减少恶意上传和错误文件类型带来的风险。

只允许部分后缀，是因为上传接口不能只靠前端限制，必须在后端做最终校验。后缀白名单虽然不是绝对安全，但能挡住大量低成本滥用，同时减少解析异常。更严格的方案还可以进一步校验 MIME type 或文件头。

如果系统多实例部署，而上传目录是本地磁盘路径 `./uploads`，就会出现“实例 A 上传的文件，实例 B 访问不到”的问题。所以多实例下更合理的方案是使用对象存储，比如 OSS、S3 或 MinIO。理论上，本地文件系统只适合单机或开发环境，不适合集群共享。

---

### 13. WebSocket 私信是怎么接入的？

**问题**
- 为什么不用纯轮询？
- WebSocket 连接建立的完整流程是什么？
- 连接建立后怎么收发消息？

**参考回答**
私信场景需要低延迟和服务端主动推送，如果用纯轮询，客户端需要不断请求服务器，不仅浪费资源，还会增加消息延迟。因此这个项目使用 WebSocket 来做实时私信。

连接建立流程大致是：前端携带 token 访问 `/ws/messages`，服务端先解析 token 并校验身份，再进行 WebSocket 升级。升级成功后，服务端把连接注册到内存中的连接管理结构里，同时启动心跳检测和消息读取循环。这样服务端就能持续接收前端发来的消息事件，并在消息落库后再推送给对应接收者。

收发消息采用事件协议，核心形式是 `type + data`。客户端发送不同类型的事件，比如发送消息、拉会话列表、拉历史消息；服务端收到后根据事件类型分发到对应处理函数，再返回标准化的响应事件。这样的设计扩展性比较好，后续要加已读回执、撤回、输入中状态等都容易扩展。

---

### 14. WebSocket 连接会在什么情况下断开？

**问题**
- token 过期怎么处理？
- 心跳检测是怎么做的？
- 断线后客户端应该怎么恢复？

**参考回答**
WebSocket 连接断开的常见原因包括 token 过期、客户端主动关闭、网络断开、服务端关闭、读取失败或写入失败。这个项目里最重要的是 token 过期和心跳机制。

token 过期后，服务端会主动发送 `auth:expired` 事件，并关闭连接。这么做是为了避免过期身份继续通过实时通道发送消息，保证鉴权一致性。

心跳检测通过定时发送 ping 实现。如果一段时间内没有 pong 或连接写入失败，说明连接可能已经失活，就会关闭并清理连接。这样可以避免服务端保存大量“假在线”的无效连接。

断线后的恢复逻辑通常由客户端来做，比较标准的做法是自动重连，并在重连后重新拉取未读消息和会话状态。理论上，WebSocket 只负责实时性，不负责最终一致性，所以即使短暂断线，也应当能通过历史消息接口补偿回来。

---

### 15. 通知中心是怎么设计的？

**问题**
- 哪些行为会产生通知？
- 通知是同步写还是异步写？为什么？
- 已读、全部已读怎么处理？

**参考回答**
通知中心通常由点赞、评论、关注等行为触发，因为这些行为都会影响被通知用户的感知。比如别人点赞了你的动态、评论了你的内容、关注了你，这些都应该产生通知。

通知最好采用异步写。原因是通知属于衍生数据，不应该阻塞主流程。比如用户刚点完赞，应该优先返回成功，而不是等通知完全写完再返回。通过 MQ 或异步任务去生成通知，可以提升主链路性能，也能更好地做重试和补偿。

已读和全部已读一般是通知中心的状态更新操作。单条已读可以修改对应通知状态，全部已读则可以批量更新未读标记。理论上，通知中心的数据模型通常是“业务事件 + 用户视图状态”的组合：业务事件决定发生了什么，状态决定用户是否看过。

---

### 16. 搜索功能是怎么实现的？

**问题**
- 搜索用户和搜索动态分别怎么做？
- 你为什么把模糊搜索改成了前缀匹配？
- 这种设计的优缺点是什么？

**参考回答**
搜索功能主要包括搜索用户和搜索动态。用户搜索一般按昵称、用户名或关键词进行匹配，动态搜索则按内容、标题、标签等字段进行检索。当前项目中为了尽量利用索引，把模糊匹配从 `%keyword%` 调整成了 `keyword%` 的前缀匹配。

这么做的原因是 `LIKE '%xxx%'` 通常很难走索引，数据量一大就容易慢，而前缀匹配更有机会使用索引，提高查询性能。缺点是召回率下降，不能搜索任意位置的关键词，只能在词头匹配时命中。

理论上，这是一种性能优先的折中方案。对于早期项目或者中小规模系统，前缀搜索已经够用；如果后面要支持更强搜索能力，可以再升级到 MySQL FullText 或 Elasticsearch。

---

### 17. 这个项目里用到了哪些中间件？

**问题**
- 鉴权中间件做了什么？
- 限流中间件怎么挂在路由上的？
- CORS 为什么要全局开启？

**参考回答**
项目里主要用到了鉴权中间件、限流中间件和 CORS 中间件。鉴权中间件负责统一解析 token，把当前用户信息注入上下文；限流中间件负责对登录、注册、发帖、评论等接口做请求控制；CORS 中间件则解决前后端分离时跨域请求的问题。

限流中间件通常是挂在具体路由上的，比如注册、登录、发布动态这些接口。这样做的好处是颗粒度更细，可以对不同接口设置不同的限流策略，而不是对全站一刀切。

CORS 之所以全局开启，是因为前后端分离架构下，浏览器会拦截不同域名、端口或协议的跨域请求。全局统一处理最方便，也避免单个接口遗漏配置。

---

### 18. 你觉得这个项目最有含金量的点是什么？

**问题**
- 是时间线、WebSocket、缓存、限流，还是 MQ？
- 哪个点最容易被面试官追问细节？
- 你准备怎么证明自己不是“照着教程做”的？

**参考回答**
我觉得这个项目最有含金量的是三块：第一是时间线推拉结合，第二是 WebSocket 私信，第三是 Redis + MQ 的工程化设计。因为这三块不是单纯做功能，而是需要你真正理解高并发系统的结构：写扩散和读扩散的平衡、实时消息推送和在线连接管理、缓存和异步的协同。

最容易被追问细节的是时间线和 WebSocket。时间线会被追问为什么这样分发、怎么去重、怎么分页、怎么处理大 V；WebSocket 会被追问在线连接怎么管理、多实例怎么推送、断线怎么补偿、token 过期怎么处理。

要证明自己不是照教程做的，最好的方式不是死背概念，而是能把链路讲清楚：请求怎么进、数据怎么落库、缓存怎么更新、消息怎么发、失败怎么兜底、哪里会出问题、怎么修复。只要这些细节讲得出来，面试官通常就能判断你是真的做过。

---

## 二面：业务细节 + 高并发 + 一致性

### 1. 你怎么讲清楚“发动态 -> 进入时间线”的全链路？

**参考回答**
发动态的全链路可以这样讲：用户请求进入后，先经过鉴权和限流，确认身份合法且没有频率过高的问题。然后 handler 负责解析请求参数，service 负责执行核心业务逻辑，先把动态写入数据库，保证数据持久化。写入成功后，根据作者的粉丝规模判断是普通用户还是大 V：如果是普通用户，就把 feed 推到粉丝 inbox；如果是大 V，就只写自己的 outbox，读取时再聚合。最后根据需要，可能还会触发通知、缓存失效或 MQ 异步任务。

失败处理上，要区分哪一步能重试、哪一步不能重试。比如数据库写入失败不能直接跳过，必须返回错误；缓存写入失败通常可以容忍，后续通过回源补偿；MQ 发送失败可以做降级或者补发，但不能影响主流程成功返回。这个思路本质上就是“主链路优先，副作用异步化”。

---

### 2. 时间线推拉混合的边界是怎么定的？

**参考回答**
边界通常是通过粉丝数阈值来定的。原因很简单：粉丝少的用户适合写扩散，因为分发成本低；粉丝多的大 V 如果也走写扩散，会把系统写爆。项目里通过配置项定义了大 V 阈值和普通推送上限，这样既有灵活性，也方便后续根据业务规模调整。

阈值设大了，会有更多用户走拉模式，导致读取时合并成本上升；阈值设小了，会有更多用户走推模式，导致写扩散压力增加。所以这个值本质上是一个“写成本”和“读成本”的平衡点，应该根据真实流量、粉丝分布和机器资源来调。

理论上，它不是一个固定真理，而是一个运维参数，应该可配置、可观察、可调优。

---

### 3. 你如何保证时间线不重复、不丢失？

**参考回答**
时间线不重复，关键在于两个地方：一是分发时不要重复写入同一个 inbox；二是读取时要做去重。比如一个 feed 可能来自 outbox、inbox 或其他聚合来源，如果不去重，前端就可能看到重复内容。通常会使用 feedID 作为唯一标识，在合并后通过 map 或 set 去重。

时间线不丢失，关键在于主数据一定要落库，缓存和 inbox/outbox 是加速层，不是唯一真相。也就是说，动态首先要进数据库，然后再考虑同步到 Redis。即使 Redis 短暂失败，也可以通过数据库回源恢复。

用户取关后，旧内容是否删除，取决于业务策略。一般可以不强制删除历史内容，因为已经出现在用户时间线里的内容属于历史记录；但如果业务要求严格，也可以在读取时过滤掉已经取消关注的作者内容。

新关注后要不要补历史内容，通常看产品定义。大多数社交产品不会补很久之前的全量历史，因为成本太高；如果要补，也往往只补最近一段时间的内容。

---

### 4. 你如何处理消息顺序问题？

**参考回答**
消息顺序问题是实时系统的典型难点。网络延迟、客户端多端登录、并发发送都可能导致消息顺序和用户操作顺序不完全一致。这个项目里，数据库中的消息顺序通常以创建时间和自增 ID 为准，WebSocket 只是负责“尽快送达”，不是唯一顺序来源。

如果多个客户端同时在线，展示顺序最好以服务端生成的消息 ID 或时间戳排序，而不是完全相信前端收到的先后顺序。因为前端收到的顺序可能受网络影响。

理论上，实时消息系统更强调“最终一致”和“用户感知顺序大致正确”，而不是要求所有链路绝对同步。只要服务端有统一的消息落库顺序，前端再按服务端数据排序，通常就能满足业务需求。

---

### 5. WebSocket 私信的完整链路是什么？

**参考回答**
完整链路是这样的：用户先发起 WebSocket 连接，请求中携带 token，服务端校验身份后完成升级。连接成功后，服务端把该连接注册到内存中的用户连接表。客户端发送消息时，消息会以事件格式传给服务端，服务端先做限流和参数校验，再调用 message service 处理业务。业务层一般会先落库，确保消息持久化，再尝试向接收者推送实时消息。发送成功后，再向发送者返回 ack，告诉客户端这条消息已经被系统接受并处理。

如果推送失败但落库成功，消息仍然算成功，因为数据库才是最终真相，推送只是加速到达。若落库失败，则必须返回错误，前端可以通过重试或状态提示让用户感知失败。

这个顺序体现的是一个典型原则：**先保证数据不丢，再追求实时性**。

---

### 6. 在线连接是怎么管理的？

**参考回答**
当前连接是存在进程内内存里的，使用一个以用户 ID 为 key 的映射结构保存在线连接。这样做的好处是简单、速度快、适合单机或单实例部署。支持多端登录时，一个用户 ID 下可以挂多个连接，因此同一用户在多个设备上都能收到消息。

但这种设计天然依赖单机状态。单机模式下，某个用户连在哪台机器，这台机器就知道；多实例部署下，每台机器只知道自己维护的连接，不知道别的机器上的连接，所以跨实例推送会失效。这也是为什么 WebSocket 是分布式部署里最容易出问题的模块之一。

理论上，连接管理要么放到统一网关，要么通过消息总线做跨实例分发，不能只靠本地内存。

---

### 7. 如果系统改成多实例部署，WebSocket 会遇到什么问题？

**参考回答**
最大的问题是“连接不共享”。实例 A 上建立的 WebSocket 连接，实例 B 并不知道，所以实例 B 即使收到了某个用户的新消息，也无法直接找到这个用户在 A 上的连接并推送过去。

解决办法一般有两种：一种是引入消息总线，比如 Redis Pub/Sub 或 RabbitMQ，把“给某个用户推送消息”的事件广播到所有实例，哪个实例持有该用户连接，哪个实例就负责推送；另一种是把 WebSocket 单独拆成网关层，所有实时连接都由网关统一管理，业务服务只负责发布事件。

如果是项目现阶段，我会优先推荐 Redis Pub/Sub 或 MQ 广播，因为改动相对小，能快速实现多实例推送；如果后期规模更大，再考虑专门的 WS 网关。

---

### 8. 消息的撤回是怎么实现的？

**参考回答**
消息撤回本质上是一个状态更新问题。发送出去的消息一旦落库，就不应该物理删除，而是把状态改成“已撤回”或“不可见”。这样做的好处是保留审计痕迹，同时便于双方客户端同步状态。

撤回逻辑一般由发送方触发，服务端校验权限后修改消息状态，并通过实时通道通知双方客户端刷新展示。前端展示时不再显示消息正文，而是显示“该消息已撤回”之类的占位文本。

如果消息是 ABCD，撤回 B 后，前端通常仍保留 B 的位置，但内容替换为撤回提示，这样可以保持上下文顺序不乱。理论上，撤回不是删除消息记录，而是改变消息生命周期状态。

---

### 9. 群聊场景下怎么做扩展？

**参考回答**
群聊和私聊相比，复杂度会明显上升。群聊的核心问题是广播、成员管理和大规模在线推送。不能简单地把每条消息同步推给所有成员，否则一个大群很容易拖垮系统。

比较合理的做法是：消息先落库，再写入群消息流；在线成员通过订阅或分发机制获取消息；离线成员通过拉取历史补偿。@某人的功能一般需要对群成员做索引或关键词匹配，不能每次都全量扫描成员列表，否则性能会差。

群成员列表适合缓存，消息列表适合分页存储。理论上，群聊的关键是避免“每条消息都同步打爆每个连接”，而是把广播和落库解耦。

---

### 10. 大量消息场景下，如何保证查询性能？

**参考回答**
会话列表通常按最近消息时间排序，历史消息则按时间倒序分页查询。为了保证性能，必须建好索引，比如会话 ID、用户 ID、创建时间的联合索引。查询时尽量只取必要字段，避免回表过多。

历史消息分页建议使用游标或基于消息 ID 的分页，而不是深 offset 分页。因为 offset 越大，数据库扫描成本越高。会话列表的缓存也很重要，可以把最近活跃会话放 Redis 或单独的摘要表里，减少每次都扫消息表。

理论上，这类场景的目标不是“查全量”，而是“查最近且高频访问的数据”，所以索引、分页方式和冷热分层非常关键。

---

### 11. 评论、点赞这类热点写操作怎么防止打爆数据库？

**参考回答**
热点写操作应该尽量减少同步阻塞。点赞、评论、转发这类操作如果每次都直接写数据库，流量一大就容易把数据库打满。常见做法有两种：一种是先写缓存或消息队列，再异步落库；另一种是保持数据库强一致写入，但配合限流、幂等和批量聚合。

这个项目里，至少应该在入口做限流，避免异常流量把核心表拖垮。对于计数类字段，比如点赞数、评论数，可以考虑先在 Redis 中聚合，再定时回刷数据库。对于必须强一致的行为，比如“是否点赞过”“是否关注过”，则要更谨慎，最好通过唯一约束或状态表保证不重复。

理论上，热点写优化的核心不是“完全不写数据库”，而是“把即时写和最终一致拆开”。

---

### 12. RabbitMQ 在项目里承担什么角色？

**参考回答**
RabbitMQ 在这个项目里主要承担异步解耦和可靠投递的角色。像通知生成、消息投递、重试、死信处理，这些都很适合放到 MQ 里。原因是它们通常不是主链路的核心返回条件，可以延后处理，从而把用户请求快速返回。

生产者发送失败时，主流程一般要么降级，要么记录日志后继续执行，不能因为 MQ 抖动就让整个接口失败。消费者处理失败后，可以进入重试队列，多次重试仍失败则进入 DLQ，方便后续排查或补偿。

理论上，MQ 解决的是“同步调用耦合太重”的问题，让系统从强耦合的串行链路变成可恢复的异步流程。

---

### 13. 你怎么保证 MQ 消息不丢？

**参考回答**
消息不丢通常需要生产端、Broker、消费端三方一起保证。生产端可以使用 confirm 机制确认消息已经到达 MQ；Broker 侧需要持久化队列和消息，防止重启丢失；消费端则要保证幂等，因为即使消息不丢，也可能重复消费。

如果消费端处理失败，可以重新入队或进入重试队列，但必须配合幂等键，否则同一条消息可能被重复执行。这个项目里如果要把消息可靠性讲清楚，可以强调：主数据先落库，异步消息作为补充；即使 MQ 出问题，也不能让主业务丢失。

理论上，“不丢”靠持久化，“不重”靠幂等，“可恢复”靠重试和死信。

---

### 14. 幂等是怎么做的？

**参考回答**
幂等要解决的是“同一操作被重复执行时，结果不变”。这个项目里容易需要幂等的地方包括点赞、关注、消息发送、MQ 消费等。常见做法有三类：数据库唯一索引、Redis 去重键、状态机条件更新。

比如 MQ 消费时，可以用 Redis `SETNX + TTL` 记录某条消息是否处理过，处理过就直接跳过。TTL 的作用是防止 key 永久占用，同时也要覆盖消息可能重试的窗口。

幂等 key 的 TTL 一般不能太短，否则重复请求还没被覆盖就失效了；也不能太长，否则 key 太多会占用资源。理论上，这个 TTL 需要根据消息重试策略和业务容忍窗口来定。

---

### 15. Redis 大 key 问题怎么理解？

**参考回答**
大 key 指的是单个 Redis key 过大，导致操作慢、内存占用高、阻塞风险上升。这个项目里最容易出现大 key 的地方就是 inbox、outbox、followers、following 这些集合。如果某个用户粉丝特别多，或者某个收件箱积累特别久，就可能变成大 key。

大 key 的风险在于：读取和删除都更慢，网络传输更大，单次操作更容易造成阻塞。解决思路通常是分片、分桶、按时间切分、按业务切分，或者限制单个集合的最大长度。

理论上，Redis 适合处理高频小粒度数据，不适合长期无限增长的大集合。随着业务变大，必须提前设计 key 的生命周期和容量上限。

---

### 16. 缓存一致性怎么保证？

**参考回答**
缓存一致性本质上是在“性能”和“准确性”之间做平衡。最常见的策略是写数据库后删缓存，而不是写数据库后直接改缓存。因为删缓存更简单，也更不容易出现并发覆盖问题。项目里如果是动态详情、用户资料等内容，通常也是写完数据库后主动失效缓存，再由下一次读取重新回源并重新缓存。

如果删除缓存失败，通常可以通过重试、异步补偿或者设置较短 TTL 来兜底。理论上，缓存不是主存储，允许短暂不一致，只要最终可以收敛。

对于关注关系、动态详情这种读取很频繁的数据，失效比更新更安全；对于一些强实时数据，则可能直接不用长缓存。

---

### 17. 读缓存和写缓存的策略有什么区别？

**参考回答**
不同数据的缓存策略是不一样的。用户资料适合长 TTL 缓存，因为读取非常频繁且修改不频繁；动态详情也适合缓存，因为大多数人刷动态时都会重复读取；关注关系适合 Set 缓存，便于判断关系和批量读取。

长缓存适合读多写少的数据，短缓存适合变化快的数据。比如动态详情可以缓存 12 小时到 24 小时，但要加随机抖动；而登录态、权限等敏感数据不适合长缓存。

“缓存只是加速层”这句话的本质意思是：缓存从来不是唯一真相，真正的数据源还是数据库。缓存的目标是提升访问效率，而不是替代数据库。

---

### 18. 你如何理解 singleflight？

**参考回答**
singleflight 解决的是缓存击穿问题，也就是某个热点 key 失效时，多个请求同时回源数据库，导致数据库被瞬间打爆。singleflight 的做法是把同一时间对同一个 key 的多个并发请求合并成一次回源，其余请求等待这一次结果。

它特别适合热点读场景，比如热门动态详情、热门用户信息、热点配置项。这样即使 key 过期，也不会出现大量并发同时穿透缓存。

如果函数执行 panic，通常要依赖调用方和封装方式去处理，不能让 panic 泄漏到大量等待请求中。理论上，singleflight 只是一个并发请求合并工具，不是完整的缓存方案，仍然要配合 TTL、回源、降级等机制使用。

---

### 19. 令牌桶限流为什么适合这个项目？

**参考回答**
令牌桶特别适合这个项目，因为社交系统里的用户操作有明显的“突发性”。比如用户短时间连发几条评论、连续发动态、反复点击登录，固定窗口容易把正常突发也限制掉，而令牌桶允许一定的突发流量，同时控制长期平均速率。

漏桶更像“匀速出水”，对突发不友好；固定窗口实现简单但边界问题明显。令牌桶则兼顾了平滑性和可用性，所以更适合登录、注册、发帖、评论这些操作。

当前项目的限流实现是基于 Redis 的分布式令牌桶，因此不是单机内存级限流，而是可以多实例共享状态的。这对于分布式部署非常重要。

---

### 20. 你怎么讲这个项目里的“分布式能力”？

**参考回答**
这个项目已经具备一部分分布式能力，比如 JWT 无状态鉴权、Redis 共享缓存和限流、RabbitMQ 异步消息，这些都天然适合多实例部署。HTTP API 层基本可以横向扩容，因为核心状态不依赖本地 session。

但它也有明确的单机依赖点，比如 WebSocket 在线连接是存在内存里的，本地上传目录也是单机文件系统。所以现在更准确的说法是：这个系统已经具备分布式部署的基础，但还没有完全消除所有单点状态。

如果要真正水平扩容，我会先改 WebSocket 分发和文件存储，因为这两个地方最容易在多实例环境中出问题。

---

### 21. 你如何处理热点用户和热点内容？

**参考回答**
热点用户和热点内容是社交系统里最容易出问题的场景。比如大 V 发帖后，会有大量粉丝同时刷新时间线，如果不做特殊处理，数据库和 Redis 都可能承压。

控制手段通常有：大 V 走拉模式减少写放大；热门 feed 做缓存；时间线读取做分页和游标；热点数据增加限流和降级；必要时做异步预热。理论上，热点问题不是靠单点优化解决的，而是要从数据分发、缓存策略、分页方式、限流和异步多个角度联合控制。

---

### 22. 如果 Redis 挂了怎么办？

**参考回答**
Redis 挂了之后，核心策略是“尽量保主链路”。如果缓存读失败，可以回源数据库；如果限流失败，可以临时降级为放行或保守拒绝；如果 inbox/outbox 无法读取，也可以部分降级为数据库分页查询。

哪些功能还能继续，要看依赖 Redis 的程度。比如缓存详情、缓存用户资料、部分限流可以退化；但如果限流和时间线分发强依赖 Redis，就要做好降级预案。优先保的是用户核心访问链路，比如登录、看首页、发帖这些关键路径。

理论上，Redis 作为加速层不能成为系统单点，必须预设“它不稳定时系统还活着”的方案。

---

### 23. 如果 RabbitMQ 挂了怎么办？

**参考回答**
RabbitMQ 挂了之后，主流程不应该完全阻塞。比如用户发动态、点赞、评论等操作，最好仍然先保证数据库落库成功，再把异步通知或推送相关的任务记录下来，后续可以通过补偿任务补发。

为了避免恢复后消息堆积冲垮系统，应该在消费者恢复时控制消费速率，配合限流、批处理和预热。理论上，MQ 挂掉影响的是“异步副作用”，而不是主数据正确性，所以主链路必须能独立完成。

---

### 24. 如果数据库慢 SQL 增多了怎么办？

**参考回答**
慢 SQL 排查通常先从慢日志入手，找到最耗时、最频繁的语句，再看执行计划，确认有没有走索引、有没有回表、有没有 filesort、有没有大范围扫描。这个项目里比较典型的慢点包括时间线查询、评论分页、搜索、会话列表等。

优先优化的顺序一般是：先看索引是否正确，再看 SQL 写法是否合理，再看分页策略是否有深 offset 问题。时间线已经采用游标思路，这是非常好的方向。

理论上，数据库优化的核心是减少扫描、减少回表、减少锁竞争，而不是单纯加机器。

---

### 25. 如果用户量变大，你觉得最先出问题的会是哪一块？

**参考回答**
我认为最先出问题的很可能是 WebSocket 和时间线读取，其次是热点动态和缓存集合。原因是社交系统的压力通常不平均，少数热点用户和热点内容会带来极高的瞬时并发。

WebSocket 的问题在于长连接占资源，而且多实例时连接状态难共享；时间线的问题在于需要合并、去重、分页；热点动态的问题在于读取放大非常明显。相比之下，RabbitMQ 和 Redis 如果设计得好，短期内会更稳一些。

容量预估时，我会从在线人数、日活、单用户关注数、单帖平均粉丝数、消息频率、时间线刷新频率这几个维度拆开算，而不是只看总用户量。

---

## 三面：系统设计 + 排障 + 工程化

### 1. 让你重新设计这个社交系统，你会怎么分层？

**参考回答**
如果重新设计，我会保留现在的核心分层思想，但会进一步强调边界清晰。比如鉴权、用户、内容、消息、通知、搜索、文件存储可以作为相对独立的领域模块。对于当前规模，我仍然会保留单体为主，因为单体更容易保证开发效率和调试效率，尤其是对个人项目来说过早拆微服务并不划算。

真正需要拆出去的，通常是实时消息网关、文件存储、通知分发、搜索服务这些天然独立且扩展性要求高的模块。也就是说，我会坚持“先单体后拆分”的思路，而不是一开始就把系统拆得过细。

---

### 2. 你怎么设计一个完整的短链系统？

**参考回答**
短链系统的核心是长链到短链的映射、短链跳转、访问统计和防冲突。生成时可以用自增 ID、雪花 ID 或哈希编码，再做 Base62 编码得到短串。跳转时通过短串查到原始长链，然后 301/302 重定向。

如果要支持统计，需要记录访问次数、来源、设备、地区等信息，通常会异步写入日志或 MQ，避免阻塞跳转链路。热 key 问题可以通过缓存、分片和预热来解决。碰撞问题则要通过唯一约束、校验重试或更安全的生成策略规避。

这类题的重点是展示你对“映射、缓存、统计、容错”的理解，而不是单纯会说 Base62。

---

### 3. 你如何理解“可观测性”？

**参考回答**
可观测性就是系统出了问题之后，你能不能快速知道“哪里坏了、坏到什么程度、为什么坏”。它通常包括日志、指标和链路追踪三部分。对这个项目来说，至少应该关注 QPS、错误率、延迟、Redis 命中率、MQ 堆积、数据库慢 SQL、WebSocket 在线数等指标。

日志上要有请求 ID、用户 ID、关键业务 ID、错误栈和外部依赖异常。这样一旦出现用户反馈，就能快速定位到具体链路。

理论上，可观测性不是“有日志就行”，而是要让系统状态可量化、可追踪、可恢复。

---

### 4. WebSocket 长连接的稳定性怎么保障？

**参考回答**
稳定性主要靠四件事：心跳、超时、重连和资源清理。心跳用于判断连接是否还活着；超时用于防止读写无限挂起；重连用于网络抖动后的自动恢复；资源清理用于防止连接断了但服务端还持有引用。

服务端在连接关闭后要及时注销连接并释放内存，否则会有泄漏。客户端则应该在断线后自动重连，并且重连成功后补拉未读消息，确保体验连续。

理论上，WebSocket 是长连接，不是永久连接，所以要接受断线重连是常态，而不是异常。

---

### 5. 你的消息协议是怎么设计的？

**参考回答**
当前消息协议采用 `type + data` 结构，这样的好处是通用、可扩展、简单清晰。`type` 用来区分事件类型，比如发送消息、获取会话、获取历史；`data` 里放具体请求参数。服务端收到后按 `type` 分发到不同处理函数即可。

这种设计的优点是后续新增协议不需要大改，只要增加一个事件类型即可。缺点是需要定义好事件规范，否则前后端容易出现字段不一致。对于图片、语音、撤回、已读回执等扩展，也可以继续沿用这个结构。

理论上，这是一种典型事件驱动协议设计，适合实时交互场景。

---

### 6. 聊天系统如何支持消息撤回、已读、未读？

**参考回答**
消息撤回通过状态字段控制，已读和未读通过用户维度的状态表或计数器维护。已读回执一般由客户端在展示消息后上报，服务端更新对应消息或会话的已读状态，并向对方同步。未读数通常可以实时计算，也可以缓存聚合，取决于系统规模。

撤回和已读都属于消息生命周期管理。撤回不会改变历史事实，但会改变当前可见性；已读不会改变消息内容，但会改变会话状态。理论上，这些状态应当可追踪、可恢复、可同步。

---

### 7. 如何保证聊天和通知的顺序一致性？

**参考回答**
最好有统一的事件源。比如消息发送先落库，再由同一个业务事件驱动通知和实时推送，这样顺序更容易统一。如果先发通知再落库，或者不同系统各自独立生成事件，顺序就容易乱。

如果出现乱序，前端应以服务端时间戳和消息 ID 为准进行排序。通知和聊天不一定要求绝对同序，但要保证用户看到的体验合理。理论上，分布式系统里“绝对顺序”成本很高，通常追求的是局部顺序和最终一致。

---

### 8. 你为什么做 L0-L3 分层记忆？

**参考回答**
如果把这个问题放到项目设计里，本质上是在问“为什么不把所有状态都放一层缓存或一张表里”。分层记忆的意义是把不同频率、不同成本、不同生命周期的数据放到不同层级。热数据放近一点，冷数据放远一点；高频小数据用短链路，低频大数据用长链路。

单层缓存的问题是所有数据都一样处理，容易造成成本不均。分层的代价是逻辑更复杂，需要有明确的数据迁移、失效和回源机制。理论上，分层的核心是“让最常用的数据走最短路径”。

---

### 9. 如果让你优化 AI 回复质量，你会怎么做？

**参考回答**
如果把这个问题延伸到答题思路上，我会先控制上下文长度，再控制无关信息噪音，最后优化回答结构。简单说就是：先抓关键事实，再给出结论和解释，避免信息太散。

如果一定要套到项目里，可以理解成“把最相关的业务链路讲清楚，把无关实现细节收敛掉”。这样既节省 token，也提升表达质量。理论上，好的回答和好的工程设计很像，都是要控制复杂度。

---

### 10. 如果这个项目上线后，出现“消息延迟但最终能到”的问题，你怎么排查？

**参考回答**
我会先区分是应用延迟、MQ 堆积还是网络抖动。第一步看日志和指标，确认消息是在哪个环节慢了：是入队慢、消费慢，还是 WebSocket 推送慢。然后再看 RabbitMQ 队列长度、消费者数量、失败重试次数、WebSocket 在线数和服务端 CPU/内存情况。

如果 MQ 堆积高，说明消费跟不上；如果消费正常但推送慢，可能是 WebSocket 连接数过多或单连接写阻塞；如果消息最终能到但延迟大，可能是网络波动或消费者限速。理论上，排查这种问题要把链路拆开看，不要一上来就猜“消息丢了”。

---

### 11. 如果出现“有些用户收不到通知”的问题，你怎么排查？

**参考回答**
我会按链路逐层排查：先看业务事件有没有生成，再看 MQ 有没有投递，再看消费者有没有消费，再看数据库和缓存里有没有通知记录，最后看前端是不是展示逻辑有问题。这样可以快速定位问题是在生产端、消息层、消费端还是展示端。

如果能加请求 ID、业务 ID 和消息 ID，排查会更快。理论上，通知系统属于“产生一次、展示多次”的场景，所以每个环节都要可追踪，不能只看最终页面。

---

### 12. 如果出现“缓存命中率下降”的问题，你怎么处理？

**参考回答**
先判断是整体命中率下降还是某类 key 下降。整体下降可能是 TTL 过短、缓存策略变化、流量模式变化；局部下降可能是热点 key 失效、key 设计问题或者数据更新过于频繁。

然后看是否存在雪崩前兆，比如大量 key 同时过期、回源压力升高、数据库 QPS 突然上升。再看是否有 key 被频繁删除导致缓存无法稳定命中。理论上，命中率下降并不一定是坏事，要结合业务实时性和一致性一起看，但如果伴随数据库压力上升，就要立刻处理。

---

### 13. 如果出现“数据库连接池打满”的问题，你怎么处理？

**参考回答**
我会先止血，再分析。止血可以通过限流、降级、扩容、临时熔断热点接口来做。分析时重点看慢 SQL、事务范围、连接泄漏、请求堆积和重试风暴。

如果是慢 SQL，优先加索引和优化查询；如果是事务太长，缩短事务范围；如果是连接泄漏，要检查是否有未关闭的 rows、事务或外部调用占着连接不释放。理论上，连接池打满往往不是“连接池不够大”，而是“连接使用太久或太多慢请求堆积”。

---

### 14. 你在这个项目里做过哪些性能优化？

**参考回答**
比较有代表性的优化包括：时间线采用推拉结合，避免大 V 写爆；Redis 做用户资料、动态详情和关注关系缓存；令牌桶限流保护核心接口；搜索改成前缀匹配以更好利用索引；时间线分页使用游标思路减少深分页问题。

其中收益最大的一般是时间线和缓存，因为它们直接影响用户高频访问路径。为了工程稳定性做的优化，比如限流、TTL 抖动、MQ 重试、幂等控制，虽然不一定立刻提升指标，但对系统抗压能力非常重要。

---

### 15. 你怎么证明自己对这个项目是“真的懂”而不是“只会讲”？

**参考回答**
我会用链路和边界来证明。比如从“用户发动态”开始，我能讲清楚请求怎么进来、鉴权怎么做、数据怎么落库、Redis 怎么更新、MQ 怎么异步、WebSocket 怎么推送、失败怎么兜底、为什么普通用户和大 V 处理不同、分布式下哪些地方会出问题。

如果能讲清楚一个真实故障场景，比如 Redis 挂了怎么办、MQ 堆积怎么办、WebSocket 跨实例推送怎么办、缓存雪崩怎么办，那就说明我不仅知道功能，还知道系统的风险和边界。

---

### 16. 你认为这个项目还缺什么？

**参考回答**
从功能上看，还可以补消息撤回、已读回执、通知聚合、搜索升级、对象存储、后台管理等。从工程上看，还可以补监控、日志链路、健康检查、优雅停机、压测脚本、自动化部署和更完整的容灾方案。

如果继续往深里补，最值得做的是 WebSocket 分布式推送、文件存储迁移到对象存储、以及消息和计数的异步聚合。这些都是能明显提升“准生产级”感觉的地方。

---

### 17. 你最后想把这个项目包装成什么级别？

**参考回答**
我会把它包装成“接近生产级的社交系统学习项目”，而不是简单的课程 Demo。因为它已经覆盖了用户、内容、关系、时间线、缓存、限流、MQ、WS 这些核心模块，也有一定的分布式和可靠性设计。

面试时我会重点突出三件事：业务深度、工程稳定性、分布式思维。不要只讲“我做了很多接口”，而是讲“我怎么解决性能问题、怎么保证一致性、怎么处理异常、怎么让系统能扩展”。

---

## 面试时的答题原则

1. 先讲结论，再讲原因。
2. 结合项目真实实现，不要胡编。
3. 每个核心模块都准备“为什么这么设计”。
4. 面试官常追问的重点是边界问题：多实例、缓存失效、消息丢失、WebSocket 断连、热点 key。
5. 尽量把回答落到具体链路、具体 key、具体中间件、具体失败处理上。

---

## 建议你重点背的 10 个问题

1. 项目整体架构是怎么分层的？
2. 时间线为什么要推拉结合？
3. 普通用户和大 V 的时间线策略有什么区别？
4. WebSocket 私信完整链路是什么？
5. 在线连接是怎么管理的？多实例会有什么问题？
6. Redis 在项目里做了哪些事？
7. RabbitMQ 在项目里解决了什么问题？
8. 令牌桶限流为什么这样设计？
9. 缓存一致性和幂等怎么保证？
10. 如果要支持真正的分布式部署，你会先改哪块？

---

## 最后一句

这份题单的目标不是“背答案”，而是把项目讲成一条完整链路：**为什么这么设计、怎么实现、出了问题怎么兜底、有什么边界和代价**。你只要把这些问题讲清楚，面试官通常就会认为你是真的做过这个项目。